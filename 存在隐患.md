## 一、低危 / 次要问题

1. **低危**：`GET /api/exams/<id>` 学生可查看**未受邀**考试的标题、时间、题型统计、题数（邀请码已隔离、不含答案）——敏感度低的元信息泄露。
2. **低危**：`GET /api/question-types`、`/api/difficulties` 无任何鉴权，仅返回静态枚举，无敏感数据。
3. **低危**：传统提交流 [exam\_execution/routes.py:347](file:///d:/Users/Documents/PythonTest/exam_system_Beta/exam_execution/routes.py#L347) 把 AI 批改异常 `str(e)[:50]` 直接 flash 给学生（API 版 v3.4 已修，模板版漏了）。
4. **健壮性 Bug**：[api/result\_routes.py:171](file:///d:/Users/Documents/PythonTest/exam_system_Beta/api/result_routes.py#L171) 使用了 `logging.warning(...)` 但文件未 `import logging`——题目被删除时人工评分接口会抛 NameError 被兜底成 500，应改为 `logger.warning`。
5. **低危**：`GET /api/profile/study/<student_id>` 任意教师可查任意学生的跨考试学习画像，无"我的学生/我的考试"范围校验（聚合数据，敏感度低）。

## 二、已核实无漏洞的模块

- **API 蓝图**：result（学生仅查自己、教师须为创建者）、execution（会话归属+状态校验+不返回答案）、student、chapter、exam 写操作（均教师+creator）、notification（v3.4 已修）、question/major/course（v3.4 已加教师校验）、ai（教师校验）。
- **竞赛/悬赏模块**：competition 的 10 个教师管理端点、gamification（画像查他人限教师）、team（队长/成员校验）、play（参与者+抽题范围校验）、pk（`_player_role` 参与者校验）；bounty 的 accept/reject（仅发布者）、禁投自己、投稿列表隔离。
- **传统模板蓝图**：exam（edit/publish/delete/invitation/invite/results 等全部 `@login_required` + 教师 + `creator_id` 校验；join 显式处理匿名）、exam\_execution（全部校验学生身份+会话归属+防重复提交）、result（成绩归属/解析归属/人工评分教师+creator+会话归属）、question\_bank 全部蓝图（所有写操作限教师）。

***

