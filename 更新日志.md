# 智汇学场 — 一体化智能学习平台 更新日志

> 记录项目从搭建到当前版本的每一次关键改动。

---

## 2026-06-08

### 项目初始化
- 确定技术栈：**Flask** + **SQLAlchemy** + **SQLite** + **Jinja2** + **Bootstrap 4**
- 采用蓝图（Blueprint）模块化组织路由：
  - `auth` — 用户认证（注册/登录/登出）
  - `question_bank` — 题库管理（题目CRUD、专业管理）
  - `exam` — 考试管理（创建/发布/结束考试）
  - `execution` — 学生参加考试
  - `result` — 考试成绩与记录
  - `ai_service` — AI 出题 / AI 批改
- 设计并实现核心数据模型（`database/models.py`）：
  - `User` — 用户（老师 / 学生）
  - `Major` — 专业分类
  - `Question` — 题目（含题型、难度、选项JSON、答案、解析）
  - `Exam` — 考试（含时间、时长、状态）
  - `ExamQuestion` — 考试-题目关联（含分值、排序）
  - `ExamStudent` — 被邀请参加考试的学生
  - `ExamSession` — 学生考试会话
  - `Answer` — 学生作答记录
  - `Result` — 考试成绩

### 数据库切换
- 从 PostgreSQL 切换到 **SQLite**
- 原因：缺少 `psycopg2` 驱动，导致 `ModuleNotFoundError`
- 解决：`config.py` 中 `SQLALCHEMY_DATABASE_URI` 改为 `sqlite:///exam_system.db`

### 解决循环导入问题
- **现象**：`ImportError: cannot import name 'User' from partially initialized module`
- **根因**：`app.py` → `database/models.py` → `app.py` 形成循环依赖
- **解决**：新建 `database/__init__.py`，将 `db = SQLAlchemy()` 提取到该文件中
  - `models.py` 改为 `from database import db`
  - `app.py` 中 `load_user` 函数内部延迟导入 `User`

---

## 2026-06-08（后续）

### 注册页面优化
- **邮箱字段改为可选**：
  - `models.py`：`email` 字段移除 `nullable=False` 约束
  - `auth/routes.py`：`request.form['email', '']` → `request.form.get('email', '')`（修复 `BadRequestKeyError`）
  - `templates/register.html`：去掉 `required` 属性，标签改为"邮箱（选填）"

### 首页与角色导航
- 新增 `templates/home.html`：含登录 / 注册入口的欢迎页
- `app.py` 添加首页路由：已登录用户按角色跳转
  - 老师 → 考试管理仪表盘
  - 学生 → 学生考试列表
- 导航栏（`templates/layout/header.html`）按角色显示不同菜单：
  - 老师：考试管理、题库管理、专业管理、AI出题
  - 学生：我的考试、我的成绩

### 模板补全
- 修复缺失的 HTML 模板，确保所有路由都有对应视图
- 统一使用 Bootstrap 4 + 中文界面

---

## 2026-06-09

### AI 出题功能完善
- **题型值修正**（`ai_service/routes.py`）：
  - 中文映射字典的键从 `SINGLE_CHOICE` 等大写改为 `single_choice` 等小写（匹配枚举实际值）
  - 下拉菜单选项改为中文显示（单选题、多选题、填空题、判断题、问答题）
  - 难度选项也改为中文（简单、中等、困难）
- **专业选择改为手动输入**：
  - `ai/generate.html`：将 `<select>` 下拉框改为 `<input type="text">` 输入框
  - `ai_service/routes.py`：如果输入的专业不存在则自动创建

### AI 模拟模式（`ai_service/services.py`）
- **问题**：未配置 API 密钥时 AI 出题直接报错
- **解决**：添加降级策略
  - 未配置 `DOUBAN_API_KEY` → 自动生成模拟题目
  - API 调用失败 → 同样回退到模拟模式
  - 前端提示："成功生成 N 道模拟题目！（使用模拟模式）"
- 如需真实 AI：在 `.env` 中配置有效的 `DOUBAN_API_KEY`

### 题目列表页面改进
- **筛选功能修复**（`question_bank/routes.py`）：
  - `major_id` 参数增加类型转换（`int`），防止字符串比对失败
  - 增加空值 / 空白字符串判断
- **中文显示**（`templates/questions/list.html`）：
  - 题型列：`single_choice` → "单选题"，以此类推
  - 难度列：`easy` → "简单"，以此类推

### 删除专业 Bug 修复（`question_bank/routes.py`）
- **现象**：删除专业时报错 `NOT NULL constraint failed: question.major_id`
- **根因**：SQLAlchemy 尝试将该专业下所有题目的 `major_id` 设为 `NULL`，但字段有 `NOT NULL` 约束
- **解决**：删除前检查是否有关联题目，有则提示"无法删除：该专业下还有 N 道题目"

---

## 2026-06-10

### 安全加固：删除操作改用 POST 方法
- **问题**：`delete_question` 和 `delete_major` 原先使用 GET 请求，存在 CSRF 风险
- **解决**：两个删除路由均改为 `methods=['POST']`
- 前端模板同步修改：列表页删除链接改为 `<form>` 表单提交

### 专业删除级联检查（`question_bank/routes.py`）
- 删除专业前先检查该专业下是否有关联题目
- 有题目时拒绝删除，Flash 提示“该专业下还有 N 道题目，无法删除”
- 新增 AJAX 接口 `/majors/<int:id>/info`，返回专业下题目数量 JSON

### 删除确认弹框迁移
- **问题**：删除专业的确认提示原先出现在 AI 出题页面（Flash 跨页面传递）
- **解决**：在专业管理页面（`majors.html`）重构为左右分栏布局，内置 Bootstrap Modal 删除确认弹框
- 弹框内通过 AJAX 实时显示关联题目数量，有题目时禁用删除按钮并显示警告

### 全站 UI 统一升级
- **导航栏**（`layout/header.html`）：添加 Font Awesome 图标、当前页高亮、响应式菜单
- **登录/注册/首页**：统一渐变背景、卡片式设计、Flash 消息自动消失
- **教师仪表盘**（`exam/dashboard.html`）：新增考试统计卡片（考试总数/已发布/已结束）
- **学生仪表盘**（`execution/dashboard.html`）：统一新 UI 规范
- **题目列表**（`questions/list.html`）：增加按题型筛选下拉框
- **AI 出题页**（`ai/generate.html`）：移除原来误放的删除按钮，添加加载遮罩
- **成绩页面**（`exam/results.html`、`result/detail.html`）：统一新 UI 规范

### 枚举遍历 Bug 修复（`ai_service/routes.py`）
- **现象**：AI 出题页题型下拉框出现了 `SUBJECTIVE_TYPES`、`is_subjective`、`label` 三个非题型选项
- **根因**：`dir(QuestionTypeEnum)` 返回了集合属性和方法名
- **解决**：添加 `isinstance(getattr(QuestionTypeEnum, t), str)` 过滤条件，只保留字符串常量

---

## 2026-06-11

### 新增三种主观题型
- `database/models.py`：`QuestionTypeEnum` 扩展 3 种新题型：
  - `programming`（编程题）
  - `application`（应用题）
  - `calculation`（计算题）
- 新增 `SUBJECTIVE_TYPES` 集合：`{short_answer, programming, application, calculation}`
- 新增工具方法：
  - `is_subjective(type)`：判断是否为主观题
  - `label(type)`：返回中文名称（如 `programming` → `编程题`）
- 前端同步更新：`add.html`、`edit.html`、`list.html`、`take_exam.html` 均支持新题型

### 教师人工评分功能
- **`Answer` 模型新增字段**（`database/models.py`）：
  - `manual_score`（FLOAT）：教师人工评分
  - `manual_comment`（TEXT）：教师评语
  - `needs_manual_grade`（BOOLEAN）：是否需要人工评分
- **新增属性**：
  - `effective_score`：有人工评分时取人工分，否则取 AI 分
  - `is_manual_graded`：是否已人工评分
- **AI 批改增强**（`ai_service/services.py`）：
  - `ai_grade_exam()`：主观题自动标记 `needs_manual_grade=True`
- **AI 出题 prompt 差异化**（`ai_service/client.py`）：
  - 编程题：要求输出参考代码和解题思路
  - 应用题：要求完整解答过程
  - 计算题：要求计算步骤和公式原理
- **新增路由**（`result/routes.py`）：
  - `GET /results/grading/<exam_id>`：教师人工评分-学生列表（显示主观题总数、已评/待评数量）
  - `GET/POST /results/grading/<exam_id>/<student_id>`：逐题评分表单（分数范围校验 [0, 满分]、保存评语、自动重算总成绩）
- **新增模板**：
  - `result/grading_list.html`：人工评分学生列表页
  - `result/grading_detail.html`：逐题评分表单（显示题目/作答/参考答案/AI分/评分输入框）
- **成绩页集成**：`exam/results.html` 和 `result/detail.html` 新增人工评分入口按钮

### 数据库自动迁移（`app.py`）
- 启动时自动执行 `ALTER TABLE` 为已有数据库补充新字段：
  - `answer.manual_score` (FLOAT)
  - `answer.manual_comment` (TEXT)
  - `answer.needs_manual_grade` (BOOLEAN DEFAULT 0)
- 幂等设计：字段已存在时 `try/except` 忽略错误，重复启动无影响

### 说明文档全面重构
- 完全重写 `说明文档.md`，删除与实际代码不符的虚拟文件描述
- 新增章节：
  - 模型关联关系表（含 `cascade` 策略说明）
  - 考试状态生命周期（`draft → published → ended` 状态流转）
  - 权限控制模型（教师/学生各模块权限矩阵）
  - 数据完整性与安全（CSRF、级联检查、密码哈希、幂等迁移）
  - 架构设计（蓝图注册、应用入口、AI 服务三层架构）
- 新增 6 张 UML 图（Mermaid 语法，兼容 Typora 8.14.0）：
  - 领域模型类图、考试全生命周期时序图、路由模块依赖图
  - 教师端活动流程图、学生端活动流程图、AI 评卷与人工复核流程图
- Typora 兼容性修复：
  - `flowchart TD` → `graph TD`（8.14.0 不支持 `flowchart`）
  - 移除 `<br/>` 标签，改用纯文本节点标签
  - subgraph 中文名称改为英文标识

---

## 2026-06-13

### 注册邮箱唯一约束冲突修复（`auth/routes.py`）
- **现象**：第二个不填邮箱的用户注册时报错 `UNIQUE constraint failed: user.email`
- **根因**：空邮箱存为 `''`（空字符串），SQLite 唯一约束下多个 `''` 会冲突
- **解决**：`email = request.form.get('email', '').strip() or None`，空邮箱存 `None`（SQL NULL），SQLite 允许多个 NULL 共存

### ExamQuestion 模型缺少 relationship（`database/models.py`）
- **现象**：添加题目页面报错 `'ExamQuestion object' has no attribute 'question'`
- **根因**：`ExamQuestion` 只有 `question_id` 外键，未声明 `question` 关系属性
- **解决**：添加 `question = db.relationship('Question')`，模板中 `eq.question.content` 即可正常访问

### SQLAlchemy 2.0 LegacyAPIWarning 修复（`app.py`）
- **现象**：`load_user` 回调触发 `LegacyAPIWarning: The Query.get() method is considered legacy`
- **解决**：`User.query.get(int(user_id))` → `db.session.get(User, int(user_id))`

### 考试时间校验时区不一致修复（`exam_execution/routes.py`）
- **现象**：学生在考试时间内点击"开始"却提示"考试尚未开始"
- **根因**：`datetime.utcnow()` 返回 UTC 时间，而 `exam.start_time` 存的是本地时间，中国 UTC+8 下比较错位
- **解决**：所有时间比较统一使用 `datetime.now()`（本地时间），共修改 4 处：
  - 考试时间校验（`now < exam.start_time`）
  - 创建考试会话的 `start_time`
  - 提交考试的 `end_time`
  - 成绩记录的 `submitted_at`

### SQLite 数据库锁超时修复（`config.py` + `exam_execution/routes.py`）
- **现象**：学生提交考试时报错 `database is locked`
- **根因**：AI 批改需多次 API 调用（约 10 秒），期间持有 SQLite 写事务，而 SQLite 默认锁超时仅 5 秒
- **解决**：
  - `config.py`：增加 `SQLALCHEMY_ENGINE_OPTIONS = {"connect_args": {"timeout": 30}}`，锁超时提升至 30 秒
  - `submit_exam`：确保先 commit 考试会话状态（释放锁），再调用 AI 批改

### 首页导航栏精简（`templates/home.html`）
- 移除右上角的"登录"和"注册"按钮，保留页面中央卡片内的入口按钮

### LegacyAPIWarning 全面修复（SQLAlchemy 2.0 兼容）
- `app.py`：`load_user` 回调 `User.query.get()` → `db.session.get()`（此前已修复）
- `ai_service/services.py`：3 处 `Model.query.get()` → `db.get()` / `_db.session.get()`
- `exam_execution/routes.py`：3 处 `Model.query.get()` → `db.session.get()`
- `result/routes.py`：4 处 `Model.query.get()` → `db.session.get()`
- 全项目 0 处遗留 `.query.get()` 调用

### 学生端计时防作弊机制
- **倒计时同步**：
  - 服务端计算剩余秒数（基于 `exam_session.start_time` + `exam.duration`）
  - 前端 JS 倒计时与服务端同步，避免客户端修改时间作弊
  - 剩余 5 分钟变黄色警告，剩余 1 分钟变红色闪烁
  - 倒计时结束自动提交试卷
- **切屏检测**：
  - 监听 `document.visibilitychange` 事件，检测用户离开页面
  - 每次切屏通过 AJAX 上报服务端（`/exam/<id>/report_switch`）
  - 切屏后弹出警告弹框，显示当前切屏次数
  - 切屏达到 3 次自动提交试卷
- **模型新增字段**：`ExamSession.switch_count`（切屏次数，默认 0）
- **数据库自动迁移**：`app.py` 添加 `exam_session.switch_count` 字段
- **页面重构**：固定顶部栏显示倒计时 + 切屏次数徽章

### AI 出题 `scoped_session` 参数调用修复（`ai_service/services.py`）
- **现象**：AI 出题报错 `'scoped_session' object has no attribute 'session'`
- **根因**：`ai_generate_questions` 和 `ai_grade_exam` 的参数 `db` 实际传入的是 `db.session`（scoped_session），但代码中误用了 `db.session.get()`（双重访问 `.session`）
- **解决**：
  - `ai_generate_questions`：`db.session.get()` → `db.get()`（参数已是 session）
  - `ai_grade_exam`：同上
  - `ai_generate_analysis`：参数为 `None`，改用局部导入 `_db.session.get()`

### AI API 超时与重试机制（`ai_service/client.py`）
- **问题**：生成多道题目时 API 响应超过 30 秒导致 `Read timed out`
- **解决**：
  - 超时时间从 30 秒提升至 60 秒
  - 新增自动重试机制（`max_retries=2`），超时后自动重试一次
  - 非超时类错误（4xx/5xx/连接失败）不重试，直接报错
  - 日志打印每次请求尝试次数，方便排查

### AI 出题枚举值大小写不一致修复（`ai_service/routes.py`）
- **现象**：AI 生成的单选题筛选时选“单选题”查询不到
- **根因**：下拉框 `value` 使用了 `dir()` 返回的属性名（`SINGLE_CHOICE` 大写），而非实际枚举值（`single_choice` 小写），导致存入数据库的题型值与筛选查询不匹配
- **解决**：
  - 下拉框生成：`'value': t` → `'value': getattr(QuestionTypeEnum, t)`
  - 映射字典键从小写改为大写：`'SINGLE_CHOICE': '单选题'` → `'single_choice': '单选题'`
  - 难度映射同步修复：`'EASY': '简单'` → `'easy': '简单'`
- **数据迁移**（`app.py`）：启动时自动将已有题目的大写题型/难度值转为小写

### 考试截止时间自动触发结束（`app.py`）
- **功能**：每次请求时自动检查已过 `end_time` 的考试，将状态从 `published` 改为 `ended`
- **实现方式**：`@app.before_request` 钩子，轻量级查询，无需外部定时任务
- **自动提交进行中的会话**：被自动结束的考试中所有 `in_progress` 状态的考试会话也会被自动标记为 `submitted`
- **状态提示优化**（`exam_execution/routes.py`）：学生进入考试时，区分显示“考试尚未发布”和“考试已结束”两种提示



### 考试管理「添加题目」页面全面增强（`exam/routes.py` + `templates/exam/add_questions.html`）
- **筛选功能**：新增专业下拉框 + 题型下拉框，支持组合筛选可选题目
- **已添加题目表格**：从简单列表改为表格，显示序号、题目内容、专业、题型、分值
- **取消已选题目**：每道已添加题目旁增加删除按钮（带确认弹框），移除后自动重新排序
- **随机打乱顺序**：新增「随机打乱顺序」按钮（2 道题以上可用），使用 `random.shuffle` 重排 `order` 字段
- **防重复添加**：已添加的题目通过 `notin_(added_ids)` 自动排除，可选列表标题注明「已添加的题目已自动排除」
- **新增路由**：
  - `POST /exams/<id>/remove_question/<question_id>`：从考试中移除某道题目
  - `POST /exams/<id>/shuffle_questions`：随机打乱考试中题目顺序

### 学生邀请管理增强（`exam/routes.py` + `templates/exam/invite.html`）
- **取消邀请功能**：已邀请学生表格每行新增删除按钮，支持取消单个学生的邀请
- **全局返回按钮**：返回按钮移出条件块，无论是否有可选学生都始终可见
- **模板重写**：统一 Card 布局 + Font Awesome 图标，显示用户名、邮箱（None 改为「未填写」）、邀请时间
- **已邀请自动排除**：可选学生列表标题注明「已邀请的已自动排除」，全部邀请完毕时显示成功提示
- **新增路由**：`POST /exams/<id>/remove_student/<student_id>`：取消某位学生的考试邀请

### 专业列表点击跳转题库（`templates/questions/majors.html`）
- 专业名称改为可点击超链接，点击直接跳转到对应专业的题库管理（`/questions?major_id=X`）
- 操作列新增蓝色「查看题库」按钮
- 鼠标悬停专业名称时变色并显示下划线

### 考试提交失败不跳转修复（`exam_execution/routes.py` + `templates/execution/take_exam.html`）
- **AI 批改错误处理**：`submit_exam` 中 `ai_grade_exam()` 包裹 `try/except`，AI 失败时仍创建成绩记录（分数 0），Flash 提示「成绩将待教师人工批改后公布」
- **防止重复提交**：检测会话已为 `submitted` 状态时直接跳转已有成绩页
- **前端 loading 遮罩**：点击提交/保存/倒计时归零/切屏自动提交时均弹出全屏加载动画（转圈 + 「正在提交试卷，请稍候…AI 正在批改中」）
- **替换 alert**：倒计时到零和切屏满 3 次时改用 loading 遮罩替代浏览器 alert

### 考试解析查看权限控制（`exam_execution/routes.py` + `result/routes.py` + 模板）
- **双重判断规则**：解析仅在以下任一条件满足时可查看：
  - 考试状态为 `ended`（老师手动结束或系统自动结束）
  - 当前时间已超过考试截止时间 `end_time`
- **成绩页**（`execution/result.html`）：重写为居中卡片布局，成绩按得分率着色（≥80% 绿 / ≥60% 黄 / <60% 红）；未结束时显示锁定提示「考试尚未结束，解析将在考试结束后开放查看」+ 截止时间
- **成绩列表页**（`result/my_results.html`）：每行根据考试状态动态判断，已结束显示「查看解析」按钮，未结束显示「考试结束后开放」
- **访问控制**（`result/routes.py` `view_analysis`）：同样使用双重判断，被拒绝时学生跳回成绩页而非首页
- **路由传参**：`view_result` 传入 `exam_ended` 布尔值；`my_results` 传入 `now` 和 `ExamStatusEnum`

### 学生仪表板三板块分离（`exam_execution/routes.py` + `templates/execution/dashboard.html`）
- **板块划分**：
  - 「待参加 / 进行中的考试」（蓝色竖条）：仅含 `published` 且未结束的考试，显示「开始/继续」操作
  - 「已结束的考试」（红色竖条）：已提交的考试，显示成绩 + 查看详情/解析按钮
  - 「我的成绩」（绿色竖条）：保持不变
- **数据源优化**：已结束考试从 `Result` 表获取（最可靠来源），而非仅从 `ExamStudent` 邀请记录过滤
- **活跃考试过滤**：从邀请列表中排除已提交的考试，仅显示真正待参加的
- **兜底处理**：已结束但未提交（超时自动结束）的考试也归入已结束板块
- **已结束板块特性**：直接显示得分/满分（按正确率着色）、「查看成绩」和「查看解析」双按钮



---

## 2026-06-15

### 题目批量导入/导出功能（`question_bank/routes.py` + `templates/questions/list.html`）

要下载第三方包：

```bash
pip install openpyxl
```

- **支持格式**：CSV 和 Excel（`.xlsx`）双格式，导入时自动识别文件类型
- **下载模板**（`GET /questions/template?format=csv|excel`）：
  - CSV：带 UTF-8 BOM，Excel 直接识别中文
  - Excel：带表头样式（蓝色背景 + 白字粗体）、自动列宽、冻结表头，含示例行
- **导出**（`GET /questions/export?format=csv|excel`）：按当前筛选条件导出题目
  - Excel 导出同样带有样式和冻结表头
- **批量导入**（`POST /questions/import`）：
  - 自动检测 `.xlsx/.xls` 或 `.csv` 文件格式
  - 表头列校验，缺少必填列时拒绝导入并提示
  - 专业不存在时自动创建
  - 题型/难度中文映射（「单选题」→ `single_choice`，「简单」→ `easy`）
  - 空内容/空答案行自动跳过并统计警告
- **页头按钮**：导出和模板各拆分为 CSV/Excel 两个按钮
- **导入弹框**：接受 `.csv` 和 `.xlsx` 文件，内含两种格式的模板下载链接
- **依赖处理**：`openpyxl` 用 `try/except` 导入，未安装时 Excel 功能按钮仍显示但会提示安装

### AI批改性能优化：客观题本地比对（`ai_service/services.py`）
- **原方案**：每道题均调用 AI API 批改，10 道题需 10 次 API 请求，耗时数十秒
- **新方案**：
  - **客观题**（单选/多选/填空/判断）：直接与题库答案本地比对，即时评分
    - 单选题/判断题：忽略大小写后精确匹配
    - 多选题：排序后比较（允许答案顺序不同）
    - 填空题：忽略大小写和多余空格
  - **主观题**（问答/编程/应用/计算）：仍调用 AI 语义批改，失败时给 0 分等人工评分
- **性能提升**：纯客观题考试提交后几乎即时完成，无需等待 API

### 提交/切屏交互优化（`templates/execution/take_exam.html`）
- **防重复提交**：新增 `isSubmitting` 标志，倒计时归零/切屏自动提交/手动点击三处均加锁
- **切屏弹窗与提交遮罩不再重叠**：提交时先 `$('#switchModal').modal('hide')` 关闭切屏弹窗，再显示提交遮罩
- **提交中禁用切屏检测**：`visibilitychange` 监听器在 `isSubmitting=true` 时直接返回，不再触发上报和弹窗
- **遮罩层 pointer-events: none**：阻止点击穿透，防止意外操作
- **遮罩提示简化**：「AI 正在批改中，可能需要数十秒」改为「正在自动批改中」（因为客观题已无需等待 AI）

### 答案解析页正确性判断修复（`result/routes.py` + `templates/result/analysis.html`）
- **根因**：旧版 AI 批改对每道题调 API，API 失败时 `answer.is_correct` 和 `answer.score` 保留为 `None`，模板判断 None 为 falsy 显示「错误」，得分显示「None 分」
- **修复**：`view_analysis` 路由中检测 `is_correct` 是否为 None，若是则按客观题本地比对重新判断：
  - 单选/判断：忽略大小写匹配
  - 多选：排序后比较
  - 填空：忽略大小写和空格
  - 主观题：有分数则视为正确，否则标记为待人工评分
- **模板改进**：显示每道题满分分值、成绩显示 `得分 / 满分`，未作答时显示「（未作答）」

### 仪表板「已结束的考试」成绩显示修复（`exam_execution/routes.py` + `templates/execution/dashboard.html`）
- **根因**：原模板用 `{% for r in current_user.results %}{% set my_result = r %}{% endfor %}` 查找对应 Result，但 Jinja2 内层 `{% set %}` 无法在外层作用域生效，导致 `my_result` 始终为 `None`
- **修复**：路由层构建 `result_map = {exam_id: Result}` 字典传入模板，模板直接用 `result_map.get(exam.id)` 获取 Result
- **显示逻辑优化**：
  - 有成绩且 `total_score > 0`：显示「得分 / 满分」（着色）
  - 有成绩但 `total_score == 0`：显示「待批改」
  - 无 Result 记录：显示「未作答」

### 自动结束考试时正确批改客观题（`app.py`）
- **原方案**：`auto_end_expired_exams` 仅累加 `Answer.score or 0`，但 Answer 的 `score` 字段可能为 None（未批改）
- **新方案**：在创建 Result 前对每道未批改的客观题做本地比对，设置 `is_correct` 和 `score`，主观题标记 `needs_manual_grade=True`
- 确保超时自动结束的考试也能正确计算成绩，不再出现「得分 0 / 总分 0」的空记录

---

## 2026-06-25

### 前端功能修复

#### 1. 首页按钮点击无响应修复

- **问题描述**：顶部导航的"首页"面包屑没有点击事件
- **修复方案**：
  - 在 `frontend/src/views/Layout.vue` 中添加 `goHome()` 函数
  - 根据用户角色跳转到不同首页（教师→题库管理，学生→我的考试）
  - 添加点击样式和悬停效果

#### 2. 通知功能修复

- **问题描述**：通知组件无法点击和清除，存在 `Cannot read properties of undefined (reading 'hideIndicator')` 错误
- **修复方案**：
  - 移除有问题的 `el-badge` 组件，改用自定义通知按钮
  - 添加通知数据管理和交互方法
  - 实现通知点击删除和批量清空功能

#### 3. 暗黑主题渲染优化

- **问题描述**：个人资料和系统设置页面在暗黑模式下背景色与边框显示异常
- **修复方案**：
  - 修改页面容器样式，使用 `var(--el-bg-color-page)` 作为背景色
  - 添加边框样式 `border: 1px solid var(--el-border-color)`
  - 更新全局样式，覆盖 Element Plus CSS 变量

### AI出题功能增强

#### 1. 知识点自动匹配

- 在 `ai_service/client.py` 中优化提示词模板，添加 `knowledge` 字段要求
- AI生成题目时自动添加相关知识点标签

#### 2. 答案解析增强

- 修改AI提示词，要求更详细的解析内容
- 包含解题思路、考点分析等信息

#### 3. 数据库模型扩展

- 在 `database/models.py` 的 `Question` 模型中添加 `knowledge` 字段
- 更新 API 接口返回新增字段

### API接口修复

#### 1. 请求路由重复问题

- **问题描述**：后端 Blueprint 和前端 axios 都设置了 `/api` 前缀，导致请求变成 `/api/api/questions`（404错误）
- **修复方案**：
  - 移除后端所有 API Blueprint 的 `url_prefix='/api'` 参数
  - 在 `app.py` 注册蓝图时统一添加 `/api` 前缀
  - 调整前端 axios 配置

#### 2. 数据库字段缺失修复

- **问题描述**：`Question` 模型添加了 `knowledge` 字段，但数据库表中没有该字段
- **修复方案**：
  - 在 `app.py` 中添加数据库自动迁移代码
  - 启动时自动为现有表添加新字段

### 路由架构优化

#### 1. 蓝图模块化拆分

- 将原有的 `api/routes.py` 拆分为独立模块：
  - `api/auth_routes.py` → 认证相关 API
  - `api/question_routes.py` → 题库管理 API
  - `api/course_routes.py` → 课程管理 API
  - `api/major_routes.py` → 专业管理 API
  - `api/exam_routes.py` → 考试管理 API
  - `api/student_routes.py` → 学生相关 API
  - `api/execution_routes.py` → 考试执行 API
  - `api/result_routes.py` → 成绩管理 API
  - `api/ai_routes.py` → AI 服务 API

### 修改的文件清单

| 文件路径                                | 修改内容                                      |
| --------------------------------------- | --------------------------------------------- |
| `frontend/src/views/Layout.vue`         | 添加首页跳转、通知管理功能                    |
| `frontend/src/views/UserProfile.vue`    | 修复暗黑模式样式                              |
| `frontend/src/views/SystemSettings.vue` | 修复暗黑模式样式                              |
| `frontend/src/utils/request.js`         | 调整 baseURL 配置                             |
| `frontend/.env.development`             | 更新环境变量                                  |
| `ai_service/client.py`                  | 增强AI提示词模板                              |
| `database/models.py`                    | 添加 knowledge 字段                           |
| `api/question_routes.py`                | 移除 url_prefix，返回 knowledge 字段          |
| `api/major_routes.py`                   | 移除 url_prefix                               |
| `api/course_routes.py`                  | 移除 url_prefix                               |
| `api/auth_routes.py`                    | 移除 url_prefix                               |
| `api/ai_routes.py`                      | 移除 url_prefix                               |
| `api/exam_routes.py`                    | 移除 url_prefix                               |
| `api/student_routes.py`                 | 移除 url_prefix                               |
| `api/execution_routes.py`               | 移除 url_prefix                               |
| `api/result_routes.py`                  | 移除 url_prefix                               |
| `app.py`                                | 注册蓝图时添加 url_prefix，添加数据库迁移代码 |

### 技术改进

1. **前端优化**：
   - 使用 Element Plus CSS 变量确保主题一致性
   - 添加通知空状态显示和操作反馈
   - 首页按钮添加悬停效果增强用户体验

2. **后端优化**：
   - 路由模块化，提高代码可维护性
   - 数据库自动迁移，减少手动操作
   - 统一 API 前缀管理

3. **AI功能增强**：
   - 知识点自动匹配便于题目分类和检索
   - 详细解析帮助学生理解题目背后的知识点

---

## 2026-06-27 ~ 2026-06-30

### 题目来源标签功能

#### 1. 数据库模型扩展（`database/models.py`）
- 在 `Question` 模型中新增 `source` 字段（`String(20)`，默认值 `'manual'`）
- 支持三种来源类型：
  - `manual` — 手动输入
  - `ai` — AI 生成
  - `past_exam` — 历年真题

#### 2. 数据库迁移（`migrate_source.py`）
- 创建独立迁移脚本，使用 `ALTER TABLE` 为现有数据库添加 `source` 字段
- 处理字段已存在的情况，确保幂等性

#### 3. AI 出题功能增强（`ai_service/services.py`）
- 修改 `ai_generate_questions` 函数，支持接收 `source` 参数
- AI 生成题目时根据参数设置题目来源

#### 4. AI 出题页面新增"历年真题"选项（`frontend/src/views/AiGenerate.vue`）
- 在题目来源下拉框中添加"历年真题"选项
- 前端传递 `past_exam` 参数到后端

#### 5. 题库管理页面来源标签显示（`frontend/src/views/QuestionList.vue` + `templates/questions/list.html`）
- 在题目卡片和列表中添加来源标签
- 根据来源类型显示不同颜色：
  - 手动输入：蓝色标签
  - AI 生成：紫色标签
  - 历年真题：黄色标签

### 智能组卷功能

#### 1. 后端 API 实现（`api/exam_routes.py`）
- 新增 `POST /api/exams/<id>/smart_generate` 接口
- 支持按专业、题型、难度和来源多条件筛选题目
- 自动计算各题型数量和难度分布
- 支持指定题目总数和各题型比例

#### 2. 前端页面集成（`frontend/src/views/ExamDashboard.vue`）
- 在考试卡片操作菜单中添加"智能组卷"选项
- 弹出智能组卷配置对话框，包含：
  - 专业选择下拉框
  - 题目来源筛选（全部/手动输入/AI生成/历年真题）
  - 题目总数设置
  - 各题型数量配置
  - 难度分布百分比配置
- 生成成功后自动刷新题目列表

### Vue3 考试编辑页面修复

#### 1. 路由配置修复（`frontend/src/router/index.js`）
- 添加 `/exams/:id/edit` 路由配置
- 确保考试编辑页面正确渲染

#### 2. 日期选择器初始化值修复
- 将日期字段初始值从空字符串 `''` 改为 `null`
- 解决 `TypeError: Cannot read properties of undefined (reading 'hideIndicator')` 错误

### 题库编辑功能优化

#### 1. 题型动态选择模式（`frontend/src/views/QuestionList.vue`）
- 根据题型动态显示不同的选项输入模式：
  - 单选题：单个正确答案选择
  - 多选题：多个正确答案选择
  - 填空题：文本输入
  - 判断题：是/否选择
  - 主观题：文本输入

#### 2. 媒体文件上传功能
- 在题目编辑对话框中添加媒体工具栏
- 支持插入图片、动图（GIF）、视频等媒体文件
- 实现文件预览和删除功能
- 后端新增文件上传 API（`api/file_routes.py`）

#### 3. Jinja2 模板同步优化（`templates/questions/edit.html`）
- 根据题型动态显示选项输入模式
- 添加媒体文件上传按钮

### Vue3 暗色主题全面优化

#### 1. 全局样式统一（`frontend/src/style.css`）
- 建立统一的设计令牌体系，使用 CSS 变量管理主题样式
- 优化暗色模式下的文字对比度：
  - 主文本：`#ffffff`
  - 次级文本：`#f1f5f9`
  - 辅助文本：`#cbd5e1`
  - 禁用文本：`#64748b`
- 添加完整的 Element Plus 暗色主题变量覆盖：
  - `--el-bg-color-page`、`--el-bg-color`、`--el-bg-color-secondary`
  - `--el-text-color-primary`、`--el-text-color-regular`、`--el-text-color-secondary`
  - `--el-input-bg-color`、`--el-input-border-color`、`--el-input-text-color`
  - `--el-select-dropdown-bg-color`、`--el-select-dropdown-text-color`

#### 2. 输入框和选择框暗色模式修复
- 修复 `el-select`、`el-input` 组件在暗色模式下的白色背景问题
- 使用 `!important` 确保样式优先级足够高
- 覆盖所有状态（正常、聚焦、悬停、禁用）

#### 3. 下拉菜单暗色模式修复
- 修复 `el-dropdown-menu`、`el-select-dropdown` 的白色背景问题
- 添加悬停效果和选中状态样式

#### 4. 各页面暗色模式优化

##### 登录页面（`frontend/src/views/Login.vue`）
- 优化深色背景渐变效果
- 增强玻璃态卡片模糊效果

##### 题库管理（`frontend/src/views/QuestionList.vue`）
- 优化题目卡片暗色样式
- 添加来源标签暗色主题颜色
- 优化编辑对话框暗色模式适配

##### 专业管理（`frontend/src/views/MajorList.vue`）
- 优化专业卡片暗色模式样式
- 调整灰色区域颜色风格，提高可读性

##### 考试管理（`frontend/src/views/ExamDashboard.vue`）
- 优化统计卡片和考试卡片暗色样式
- 添加智能组卷对话框暗色模式样式

##### 考试成绩（`frontend/src/views/ExamResults.vue`）
- 优化统计卡片和表格暗色样式
- 修复表格行白色背景问题
- 添加斑马纹效果和悬停高亮
- 添加返回按钮

##### 学生面板（`frontend/src/views/StudentDashboard.vue`）
- 优化考试列表项暗色样式

##### AI 出题（`frontend/src/views/AiGenerate.vue`）
- 优化表单卡片和下拉框暗色样式

##### 编辑考试（`frontend/src/views/EditExam.vue`）
- 优化表单暗色模式适配
- 添加返回按钮暗色模式样式

##### 章节管理（`frontend/src/views/ChapterList.vue`）
- 添加暗色模式样式

##### 考试答题（`frontend/src/views/ExamTake.vue`）
- 添加完整的暗色模式样式

### 修复的问题清单

| 问题 | 修复方案 |
|------|----------|
| Vue3考试编辑页面空白 | 添加 `/exams/:id/edit` 路由配置 |
| Element Plus日期选择器报错 | 将初始值从 `''` 改为 `null` |
| `@element-plus/icons-vue` 导出错误 | 将 `Image` 替换为 `Picture` |
| 暗色模式下拉框白色背景 | 添加全局 `el-select-dropdown` 暗色样式 |
| 暗色模式输入框白色背景 | 添加全局 `el-input__wrapper` 暗色样式 |
| 暗色模式表格行白色背景 | 添加表格暗色模式样式覆盖 |
| App.vue硬编码浅色背景 | 移除 `body { background: #f5f7fa; }` |
| 暗色模式文字可读性差 | 提高文字对比度，使用 `#ffffff` 和 `#f1f5f9` |

### 修改的文件清单

| 文件路径 | 修改内容 |
|----------|----------|
| `database/models.py` | 添加 `source` 和 `knowledge` 字段 |
| `ai_service/services.py` | 支持题目来源参数，优化AI提示词 |
| `api/exam_routes.py` | 新增智能组卷API |
| `api/file_routes.py` | 新增文件上传API |
| `frontend/src/style.css` | 全局暗色主题样式优化 |
| `frontend/src/App.vue` | 移除硬编码背景色 |
| `frontend/src/router/index.js` | 添加考试编辑路由 |
| `frontend/src/views/Login.vue` | 优化暗色模式样式 |
| `frontend/src/views/QuestionList.vue` | 来源标签、题型动态选择、媒体上传 |
| `frontend/src/views/MajorList.vue` | 优化暗色模式样式 |
| `frontend/src/views/ExamDashboard.vue` | 智能组卷功能、暗色模式优化 |
| `frontend/src/views/ExamResults.vue` | 表格暗色模式、返回按钮 |
| `frontend/src/views/StudentDashboard.vue` | 暗色模式优化 |
| `frontend/src/views/AiGenerate.vue` | 历年真题选项、暗色模式优化 |
| `frontend/src/views/EditExam.vue` | 暗色模式优化、返回按钮 |
| `frontend/src/views/ChapterList.vue` | 暗色模式优化 |
| `frontend/src/views/ExamTake.vue` | 暗色模式优化 |
| `templates/questions/list.html` | 来源标签显示 |
| `templates/questions/edit.html` | 题型动态选择 |

---

## 2026-07-01

### Vue3 成绩可视化图表分析

#### 1. 新建页面（`frontend/src/views/ResultAnalysis.vue`）
- 独立的成绩分析页面，通过路由 `/results/analysis` 访问
- 包含考试选择器（下拉框选择要分析的考试）
- 支持暗色模式，Element Plus 主题一致

#### 2. 统计概览卡片
- **参考人数**：蓝色图标卡片，显示参加该考试的学生总数
- **平均分**：绿色图标卡片，显示所有学生的平均得分
- **最高分**：红色图标卡片，显示该考试中的最高得分
- **及格率**：紫色图标卡片，显示及格学生的占比百分比

#### 3. 成绩分布柱状图（`Bar` 组件）
- 横轴：分数区间（如 0-20, 20-40, ..., 80-100）
- 纵轴：落在各区间的学生人数
- 使用渐变填充，动态显示分布趋势
- 暗色模式下自动切换配色方案

#### 4. 分数段占比环形图（`Doughnut` 组件）
- 分段显示：优秀(≥80)、良好(≥60)、及格(≥60且<80)、不及格(<60)
- 支持点击图例动态切换显示/隐藏
- 环形图中间显示学生总数
- 暗色模式下调整线条和背景色

#### 5. 题型得分分析图（`Bar` 组件）
- 横轴：题型名称（单选题、多选题、填空题、判断题、问答题等）
- 纵轴：该题型的平均得分
- 与满分值对比，直观显示各题型的掌握情况

#### 6. 学生成绩排名表格（`el-table`）
- 列：排名、学生姓名、得分、答题用时、状态
- 支持按得分排序（降序）
- 斑马纹效果和悬停高亮
- 按分数区间自动着色（≥80 绿 / ≥60 黄 / <60 红）
- 暗色模式下表格背景色适配

#### 7. 后端 API（`api/result_routes.py`）
- 新增 `GET /api/results/analysis/<exam_id>` 接口
- 返回数据结构：
  - `total_students`: 参考人数
  - `avg_score`: 平均分
  - `highest_score`: 最高分
  - `pass_rate`: 及格率
  - `score_distribution`: 各分数区间学生人数数组
  - `score_segments`: 优秀/良好/及格/不及格的人数和占比
  - `question_type_analysis`: 各题型的题目数、满分、平均得分
  - `rankings`: 学生排名列表（id、姓名、分数、用时、状态）
- 权限控制：仅考试创建者（教师）可访问

#### 8. 路由配置（`frontend/src/router/index.js`）
- 添加路由规则：`path: 'results/analysis', name: 'ResultAnalysis'`
- 在导航栏"成绩分析"菜单中添加入口

#### 9. 图表库依赖
- `chart.js`：核心图表库（已在前端安装）
- `vue-chartjs`：Vue3 封装组件（Bar、Doughnut 等）

### 考试删除功能统一

#### 1. 功能定位（Jinja2 模板端）
- 原设计：考试卡片底部有"删除"按钮，下拉菜单中也有"删除考试"选项，功能重复易误操作
- 新设计：统一删除入口，只保留**下拉菜单**中的"删除考试"选项

#### 2. 模板修改（`templates/exam/dashboard.html`）
- 移除考试卡片底部的删除按钮及其所在的操作块
- 下拉菜单保留：`编辑`、`查看成绩`、`智能组卷`、`删除考试`
- "删除考试"为红色 danger 样式，带确认弹框

#### 3. Vue3 端同步（`frontend/src/views/ExamDashboard.vue`）
- 考试卡片操作菜单保留"删除考试"选项
- 删除时确认弹框提示"确定要删除考试「XXX」吗？"
- 删除成功后显示成功消息并刷新考试列表
- 暗色模式下确认弹框样式适配

### 已结束考试重复发布功能

#### 1. 后端逻辑修改（`exam/routes.py` + `api/exam_routes.py`）
- 原限制：仅草稿状态的考试可发布
- 新逻辑：`ended`（已结束）状态的考试也可重新发布
- 教师可修改考试的开始/结束时间后重新发布
- 发布前校验考试至少包含一道题目

#### 2. 前端按钮文本调整（Jinja2 + Vue3）
- 已结束考试的操作按钮文本从"查看成绩"改为"重新发布"
- 点击后调用发布接口，状态流转为 `published`
- 学生端可再次参加已重新发布的考试

### 邀请设置功能（邀请码 / 邀请链接 / QR码）

#### 1. Jinja2 模板端 —— 编辑页（`templates/exam/edit.html`）

##### A. 邀请码生成
- 勾选"生成邀请码"复选框后自动生成 8 位随机邀请码
- 卡片展示邀请码及复制按钮
- 邀请码格式：字母+数字组合，确保唯一
- 学生可在考试页面输入邀请码加入考试

##### B. 邀请链接生成
- 勾选"生成邀请链接"复选框后生成可直接访问的 URL
- 链接格式：`http://host:port/exam/join/<邀请码>`
- 卡片展示链接输入框 + 复制按钮
- 学生点击链接可直接加入考试

##### C. QR码生成
- 勾选"生成QR码"复选框后，根据邀请链接生成二维码图片
- 使用 `qrcode` 第三方包生成图片
- 卡片展示二维码图片（Base64 内嵌），学生可扫码加入考试
- 后端生成失败时提示学生先生成邀请链接

##### D. 取消勾选后内容残留修复
- **问题根因**：模板中卡片使用 `{% if exam.invitation_code %}show{% endif %}`，导致页面加载时只要数据库已有数据就自动显示，与前端复选框状态不一致
- **修复方案**：
  1. 移除模板中卡片的条件 `show` 类，默认隐藏
  2. JS 初始化时**不再**根据数据库状态显示卡片
  3. 复选框勾选时：调用后端 `generate_invitation` 接口 → 获取数据 → 显示对应卡片
  4. 复选框取消勾选时：直接移除 `show` 类，卡片隐藏
- **CSS 修复**：`.invite-card { display: none !important; }`，确保 Bootstrap 样式不会覆盖

##### E. 后端接口（`exam/routes.py`）
- 修复 `NameError: name 'jsonify' is not defined`（导入时漏写 jsonify）
- `POST /exams/<id>/generate_invitation` 接口：
  - 接收参数：`generate_code`、`generate_url`、`generate_qr`（布尔值）
  - 生成邀请码：`exam.generate_invitation_code()`（数据库已有则复用）
  - 生成邀请链接：`exam.generate_invitation_url(request.host_url)`
  - 生成 QR 码：使用 `qrcode.make(url)` + `base64` 编码
  - 返回 JSON：`{success, invitation_code, invitation_url, qr_code_data}`
- 新增 `exam/invitation.html` 邀请信息展示页

#### 2. Vue3 前端 —— 编辑页（`frontend/src/views/EditExam.vue`）

##### A. 邀请设置 UI
- 在表单底部新增"邀请设置"分隔区域
- 三个独立复选框：
  - `☐ 生成邀请码`（已生成时显示绿色标签）
  - `☐ 生成邀请链接`（已生成时显示绿色标签）
  - `☐ 生成QR码`（提示"需有邀请链接"）

##### B. 卡片动态显示
- **邀请码卡片**：显示 8 位邀请码 + 复制按钮
- **邀请链接卡片**：显示完整 URL + 复制按钮
- **QR码卡片**：显示二维码图片 + 说明文字
- 卡片与复选框状态同步：勾选→显示，取消→隐藏

##### C. 后端 API 调用
- 接口：`POST /api/exams/<id>/generate_invitation`
- 请求体：`{generate_code, generate_url, generate_qr}`
- 响应解析：`invitation_code`、`invitation_url`、`qr_code_data`
- 生成成功时显示"生成成功"提示

##### D. 暗色模式适配
- 卡片背景色使用 `var(--bg-card)` CSS 变量
- 邀请码文字色使用 `var(--accent-primary)`
- 复制按钮、分隔线均适配暗色主题

#### 3. Vue3 后端 API 新增（`api/exam_routes.py`）

##### `POST /api/exams/<id>/generate_invitation`
- 权限：考试创建者（教师）
- 流程：
  1. 检测 `generate_code=True` 且 `exam.invitation_code` 为空 → 生成邀请码
  2. 检测 `generate_url` 或 `generate_qr=True` 且邀请码已存在 → 生成邀请链接
  3. 数据库提交
  4. 根据勾选状态返回对应字段（code/url/qr_data）
  5. QR 码：使用 `qrcode` 包生成 PNG，经 base64 编码后返回
- 兼容：`qrcode` 包未安装时 QR 码字段返回 `null`，不影响其他功能

##### `GET /api/exams/<id>` 扩展
- 考试详情接口新增返回字段：
  - `invitation_code`：当前邀请码（如存在）
  - `invitation_url`：当前邀请链接（如存在）
- 前端页面加载时可显示"已生成"标签提示

### 修改的文件清单

| 文件路径 | 修改内容 |
|----------|----------|
| `api/result_routes.py` | 新增成绩分析接口 `/api/results/analysis/<exam_id>` |
| `api/exam_routes.py` | 新增 `POST /api/exams/<id>/generate_invitation`，`GET /api/exams/<id>` 返回邀请字段 |
| `exam/routes.py` | 修复 jsonify 导入，优化 `generate_invitation` 逻辑，支持已结束考试重新发布 |
| `frontend/src/views/ResultAnalysis.vue` | **新建**：成绩可视化分析页（统计卡片+柱状图+环形图+表格） |
| `frontend/src/views/EditExam.vue` | 新增邀请设置复选框、邀请码/链接/QR码卡片、复制功能 |
| `frontend/src/views/ExamDashboard.vue` | 移除卡片底部删除按钮，保留菜单中"删除考试"，新增"重新发布" |
| `frontend/src/router/index.js` | 新增成绩分析路由 `/results/analysis` |
| `templates/exam/dashboard.html` | 移除卡片底部删除按钮，统一入口到下拉菜单 |
| `templates/exam/edit.html` | 修复取消勾选卡片残留问题，CSS `!important` 强制隐藏 |
| `templates/exam/invitation.html` | 邀请信息展示模板 |

### 前端新增依赖

| 包名 | 版本 | 用途 |
|------|------|------|
| `chart.js` | ^4.x | 图表核心库（柱状图、环形图） |
| `vue-chartjs` | ^5.x | Vue3 封装组件（Bar、Doughnut） |

---

## 2026-07-12

```powershell
pip install redis flask-caching
```

### Jinja2 模板题目输入框富文本编辑功能

#### 1. 格式化工具栏（`templates/questions/add.html`）
- 新增格式化按钮：加粗（B）、斜体（I）、下划线（U）、删除线（S）
- 新增对齐方式按钮：左对齐、居中对齐、右对齐
- 新增列表按钮：有序列表、无序列表
- 新增缩进按钮：增加缩进、减少缩进

#### 2. 数学符号插入（`templates/questions/add.html`）
- 新增数学符号工具栏：
  - 基本运算：`+`、`-`、`×`、`÷`、`=`、`≠`、`≈`、`≤`、`≥`
  - 分数：`½`、`⅓`、`¼`、`⅕`、`⅙`、`⅛`
  - 根号：`√`、`∛`、`∜`
  - 希腊字母：`α`、`β`、`γ`、`π`、`θ`、`λ`、`μ`、`σ`
- 点击符号直接插入到光标位置
- 无需掌握 LaTeX 公式输入法，教师可直接点击插入

#### 3. 图标修复（`templates/questions/add.html`）
- 将不存在的 `fa-fraction` 替换为 `fa-divide`（除法符号）
- 将不存在的 `fa-square-root-alt` 替换为 `fa-square-root-variable`

### Jinja2 模板欢迎首页

#### 1. 新建首页（`templates/home.html`）
- 导航栏：左侧 Logo（学校图标 + "在线考试系统"），右侧主题切换按钮 + 登录/注册按钮
- Hero 区域：渐变背景、标题、副标题、统计数据（试题库 5000+、用户 1000+、好评率 98%）、浮动装饰形状动画
- 核心功能展示：AI 智能出题、自动批改、数据分析、安全考试（四个卡片）
- 使用流程：登录注册 → 创建考试 → 学生答题 → 查看成绩（四个步骤）
- CTA 区域：行动号召按钮
- 页脚：快速链接、联系我们

#### 2. 路由配置（`app.py`）
- 新增首页路由 `GET /`，返回 `home.html`
- 已登录用户访问首页时自动重定向：教师 → 仪表盘，学生 → 我的考试

#### 3. 主题切换功能（`templates/home.html`）
- 右上角主题切换按钮，点击切换亮/暗色模式
- 暗色模式下导航栏、卡片、文本颜色自动适配
- 使用 `localStorage` 持久化主题状态
- 修复 `ReferenceError: toggleTheme is not defined` 错误：添加 `DOMContentLoaded` 事件监听器绑定函数

#### 4. 按钮样式优化
- 登录按钮添加边框（`border: 2px solid #667eea`）
- 注册按钮改为白色字体（透明背景 + 边框）

#### 5. CDN 资源优化（`templates/home.html`）
- 移除不必要的 Bootstrap JS 和 jQuery CDN 引用
- 解决 `net::ERR_CONNECTION_REFUSED` 错误（无法连接 cdn.jsdelivr.net）

### Vue3 模板首页

#### 1. 新建首页（`frontend/src/views/Home.vue`）
- 导航栏：学校图标 + 品牌名称 + 主题切换 + 登录/注册按钮
- Hero 区域：渐变背景、标题、副标题、统计数据、浮动形状动画
- 核心功能展示：AI 智能出题（Cpu 图标）、自动批改（Document 图标）、数据分析（TrendCharts 图标）、安全考试（Medal 图标）
- 使用流程：四步流程卡片（登录注册、创建考试、学生答题、查看成绩）
- CTA 区域：行动号召按钮
- 页脚：快速链接、联系方式

#### 2. 路由配置（`frontend/src/router/index.js`）
- 新增首页路由：`path: '/', name: 'Home', component: Home`
- 更新路由守卫：已登录用户访问首页时重定向到仪表盘

#### 3. 主题切换功能
- 全局状态管理：`isDark` ref + `localStorage` 持久化
- 暗色模式下页面背景、文本、按钮样式自动适配
- 使用 `document.body.classList.toggle('dark-mode')` 切换主题类

#### 4. 图标库回退（`frontend/src/views/Home.vue`）
- 移除 `lucide-vue-next` 图标库依赖
- 统一使用 `@element-plus/icons-vue` 图标库
- 解决 `TypeError: Failed to fetch dynamically imported module` 错误
- 解决 `ENOENT: no such file or directory` 错误（`lucide-vue-next` 包损坏）

#### 5. 前端依赖清理
- 卸载损坏的 `lucide-vue-next` 包
- 清理 Vite 依赖缓存，重新优化依赖

### 修改的文件清单

| 文件路径 | 修改内容 |
|----------|----------|
| `templates/questions/add.html` | 新增格式化工具栏、数学符号插入、修复图标 |
| `templates/home.html` | **新建**：欢迎首页（导航栏、Hero、功能展示、流程、CTA、页脚、主题切换） |
| `app.py` | 新增首页路由，已登录用户自动重定向 |
| `frontend/src/views/Home.vue` | **新建**：Vue3 首页（Element Plus 图标、主题切换、暗色模式） |
| `frontend/src/router/index.js` | 新增首页路由，更新路由守卫 |

### Vue3 模板首页优化

#### 1. 首页视觉升级（`frontend/src/views/Home.vue`）
- **玻璃拟态设计**：卡片采用 `backdrop-filter: blur(10px)` 毛玻璃效果
- **极光渐变背景**：使用 CSS 多色渐变和动画效果
- **滚动触发动画**：页面元素随滚动渐入显示
- **响应式布局**：适配桌面端和移动端

#### 2. 登录/退出跳转优化
- **退出登录**：退出后自动跳转到首页
- **登录页面**：新增"返回首页"按钮，方便用户快速导航

#### 3. 注册功能完善
- **首页**：在登录和免费试用按钮旁增加注册按钮
- **登录页面**：暗色模式下新增注册链接入口

#### 4. 注册 API 接口（`api/auth_routes.py`）
- 新增 `POST /api/register` 接口
- 支持用户名、密码、邮箱（可选）和角色选择
- 返回用户信息和登录令牌

#### 5. Vue3 注册页面（`frontend/src/views/Register.vue`）
- 完整的注册表单：用户名、密码、确认密码、邮箱（可选）、角色选择（教师/学生）
- 密码强度校验：长度≥6、包含字母和数字
- 表单验证：必填项检查、密码一致性检查
- 暗色模式适配：使用 CSS 变量确保主题一致性

#### 6. 视频演示功能（`frontend/src/components/VideoModal.vue`）
- **视频模态框**：点击"观看演示"按钮弹出全屏视频窗口
- **功能幻灯片**：4 个核心功能展示（AI 智能出题、智能批改系统、数据可视化分析、安全考试监控）
- **自动播放**：幻灯片自动轮播，每 4 秒切换一次
- **播放控制**：播放/暂停按钮、进度条、幻灯片计数器、时间显示
- **动画效果**：淡入淡出、缩放等过渡动画
- **响应式设计**：适配不同屏幕尺寸

#### 6.1 视频播放器重构
- **Canvas实时动画渲染**：使用 Canvas API + `requestAnimationFrame` 实时绘制动画帧，模拟视频播放效果
- **自定义视频控件**：不依赖原生 `<video>` 控件，完全自定义实现：
  - 播放/暂停按钮
  - 上一段/下一段按钮
  - 进度条（支持点击跳转）
  - 时间显示（当前时间/总时长）
  - 音量控制滑块
  - 全屏切换按钮
  - 关闭按钮

#### 6.2 图标库统一
- **移除Element Plus图标导入**：`PauseCircle`、`SkipBack`、`SkipForward`、`Volume2`、`Fullscreen`、`FullscreenExit` 等图标在当前版本中不存在
- **改用FontAwesome图标**：通过 `main.js` 全局注册的 `FontAwesomeIcon` 组件，使用字符串图标名称（如 `fa-solid fa-play`）
- **图标映射**：
  - `fa-solid fa-play` — 播放按钮
  - `fa-solid fa-pause` — 暂停按钮
  - `fa-solid fa-backward-step` — 上一段
  - `fa-solid fa-forward-step` — 下一段
  - `fa-solid fa-volume-high` — 音量图标
  - `fa-solid fa-expand` — 全屏
  - `fa-solid fa-compress` — 退出全屏
  - `fa-solid fa-xmark` — 关闭按钮
- **FontAwesome size属性修复**：将数字类型 `:size="48"` 改为字符串类型 `size="5x"`（`lg`、`sm`、`5x`），解决 Vue 类型校验警告

#### 6.3 Canvas兼容性修复
- **自定义圆角矩形函数**：将 `ctx.roundRect()` 替换为自定义的 `drawRoundedRect()` 函数（使用 `quadraticCurveTo` 绘制），兼容所有浏览器
- **Canvas尺寸设置**：显式设置 `canvas.width = 800` 和 `canvas.height = 450`，解决动画模糊问题
- **动画优化**：使用 `requestAnimationFrame` 实现平滑动画，包含渐变背景、标题、描述和特性标签的淡入效果
- **图标绘制**：Canvas内自定义绘制CPU、文档、图表、锁四个功能图标，无需依赖外部图标库

#### 7. 图标兼容性修复
- 将不存在的图标替换为可用图标：
  - `FileText` → `Document`
  - `Play` → `VideoPlay`
  - `Pause` → 文字"暂停"/"播放"
- 解决 `SyntaxError: The requested module does not provide an export named` 错误

#### 8. 前端路由和状态管理更新
- **路由配置**（`frontend/src/router/index.js`）：新增注册页路由 `path: '/register'`
- **状态管理**（`frontend/src/stores/auth.js`）：新增注册方法 `register()`，完善用户认证逻辑

### 修改的文件清单

| 文件路径 | 修改内容 |
|----------|----------|
| `frontend/src/views/Home.vue` | 首页视觉升级（玻璃拟态、极光渐变、滚动动画） |
| `frontend/src/views/Login.vue` | 返回首页按钮、注册链接、暗色模式样式 |
| `frontend/src/views/Register.vue` | **新建**：注册页面组件 |
| `frontend/src/components/VideoModal.vue` | **新建并重构**：视频模态框组件（Canvas动画录制、完整视频控件、FontAwesome图标、Canvas兼容性修复） |
| `frontend/src/router/index.js` | 新增注册页路由，更新路由守卫 |
| `frontend/src/stores/auth.js` | 新增注册方法 |
| `api/auth_routes.py` | 新增注册 API 接口 |

---

## 2026-07-13

```powershell
pip install Flask-Limiter
pip install Flask-WTF
```

### Redis 缓存层实现

#### 1. 新增工具模块（`utils/redis_cache.py`）
- **连接管理**：`init_redis()` 初始化 Redis 连接，失败时静默降级到数据库查询
- **缓存键定义**：统一前缀 `EXAM_SYSTEM:`，支持 `questions:*`、`majors`、`courses` 等键
- **失效策略**：题目缓存 10 分钟，列表缓存 30 分钟
- **读写策略**：先查缓存，缓存未命中再查数据库并写入缓存
- **缓存清除**：`clear_cache()` 接口用于数据更新后主动清除缓存

#### 2. 缓存集成（`api/question_routes.py`、`api/major_routes.py`、`api/course_routes.py`）
- 高频读接口添加缓存：题目列表、专业列表、课程列表
- 数据更新操作（新增/修改/删除）后自动清除相关缓存

#### 3. 配置文件更新（`config.py`）
- 新增 Redis 配置：`REDIS_HOST`、`REDIS_PORT`、`REDIS_PASSWORD`、`REDIS_DB`、`REDIS_TIMEOUT`
- 默认配置本地 Redis（`localhost:6379`），无密码

### 安全层增强

#### 1. Rate Limiting（`utils/security.py`）
- **基于 Flask-Limiter**：使用 Redis 作为限流器存储后端
- **限流策略**：
  - 登录/注册：5 次/分钟
  - AI 出题：3 次/分钟
  - 文件上传：10 次/分钟
  - 默认：200 次/分钟
- **响应头**：返回 `X-RateLimit-Limit`、`X-RateLimit-Remaining`、`X-RateLimit-Reset`

#### 2. CSRF 防护（`utils/security.py`）
- **基于 Flask-WTF**：启用 CSRFProtect
- **API 豁免**：API 蓝图使用 `csrf.exempt()` 豁免（API 使用 Token 认证）
- **错误处理**：`@app.errorhandler(400)` 处理 CSRF 验证失败

#### 3. 前端请求配置（`frontend/src/utils/request.js`）
- 添加 CSRF Token 获取逻辑：`GET /api/csrf-token`
- 请求拦截器自动携带 `X-CSRF-TOKEN` 头

### 数据库路径修复

#### 1. 配置文件修正（`config.py`）
- **问题**：原配置 `sqlite:///exam_system.db` 指向项目根目录，而实际数据在 `instance/exam_system.db`
- **修复**：改为 `sqlite:///instance/exam_system.db`

#### 2. 空数据库清理
- 删除 `d:\Python_work\instance\exam_system.db`（空数据库，无数据）

### 外部资源本地化

#### 1. MathJax 公式渲染库
- **问题**：CDN `cdn.jsdelivr.net/npm/mathjax@3/es5/tex-chtml-full.js` 加载失败（`net::ERR_CONNECTION_REFUSED`）
- **修复**：
  - 前端 npm 安装：`npm install mathjax@3`
  - 复制到静态目录：`static/mathjax/es5/`（106 个文件）
  - 修改模板引用：从 CDN 改为 `/static/mathjax/es5/tex-chtml-full.js`

#### 2. Popper.js 弹窗定位库
- **问题**：CDN `cdn.jsdelivr.net/npm/popper.js@1.16.1` 加载失败
- **修复**：
  - 复制前端已安装的 `@popperjs/core` 到 `static/popper/popper.min.js`
  - 修改所有模板引用

#### 3. Bootstrap、jQuery、Font Awesome
- **问题**：外部 CDN 资源加载不稳定
- **修复**：
  - 下载到静态目录：`static/bootstrap/`、`static/jquery/`、`static/font-awesome/`
  - 下载字体文件：`static/font-awesome/webfonts/fa-solid-900.woff2/woff/ttf`
  - 修改所有模板 CDN 引用为本地路径

#### 4. 前端导入修复（`frontend/src/main.js`）
- **问题**：`import 'mathjax/es5/output/chtml/fonts/woff-v2.css'` 文件不存在，导致 Vite 构建错误
- **修复**：移除错误的 CSS 导入，依赖静态文件自动加载

#### 5. 批量替换脚本
- 创建 `tools/replace_cdn.py` 批量替换所有模板中的 CDN 引用
- 覆盖约 25 个模板文件，替换约 80 处引用

### 修改的文件清单

| 文件路径 | 修改内容 |
|----------|----------|
| `utils/redis_cache.py` | **新建**：Redis 缓存工具模块 |
| `utils/security.py` | **新建**：安全工具模块（Rate Limiting、CSRF） |
| `config.py` | 新增 Redis 配置，修正数据库路径 |
| `app.py` | 初始化 Redis 和安全模块，API 蓝图 CSRF 豁免 |
| `api/question_routes.py` | 添加题目缓存逻辑 |
| `api/major_routes.py` | 添加专业缓存逻辑 |
| `api/course_routes.py` | 添加课程缓存逻辑 |
| `frontend/src/utils/request.js` | 添加 CSRF Token 获取和携带 |
| `frontend/src/main.js` | 移除错误的 MathJax CSS 导入 |
| `templates/layout/header.html` | MathJax 和资源引用改为本地路径 |
| 所有 Jinja2 模板（约 25 个） | CDN 引用替换为本地路径 |
| `static/mathjax/` | **新增**：MathJax 本地文件 |
| `static/popper/` | **新增**：Popper.js 本地文件 |
| `static/bootstrap/` | **新增**：Bootstrap 本地文件 |
| `static/jquery/` | **新增**：jQuery 本地文件 |
| `static/font-awesome/` | **新增**：Font Awesome 本地文件和字体 |

---

## 2026-09-05

```powershell
pip install -r requirements.txt
```

### 竞赛竞技化三期 — Socket.IO 实时推送 + 段位/赛季/勋章/战队/学情画像

#### 1. 后端新增 `competition/` 模块（13 个文件）

| 文件 | 功能 |
|------|------|
| `competition/models.py` | 11 张表：competition / competition_question / competition_participant / competition_answer / pk_battle / pk_answer / user_points_profile / season_meta / season_archive / user_badge / team / team_member |
| `competition/routes.py` | 竞赛管理 API（CRUD / 发布 / 题目管理 / 报名） |
| `competition/play_routes.py` | 限时赛参赛（加入 / 开始 / 答题 / 交卷 / 超时自动结算） |
| `competition/pk_routes.py` | 1v1 PK（发起 / 接受 / 大厅 / 答题 / 结算） |
| `competition/gamification.py` | **核心结算层**：段位定义 + 积分结算 + 勋章校验 |
| `competition/season.py` | 赛季惰性归档（CAS 抢锁保证并发安全） |
| `competition/gamification_routes.py` | 段位 / 赛季 / 勋章 / 学情画像 API |
| `competition/team_routes.py` | 战队 CRUD（创建 / 加入 / 退出 / 转让 / 解散） |
| `competition/scoring.py` | 竞赛计分（base + speed 双层） |
| `competition/leaderboard.py` | 榜单聚合 |
| `competition/realtime.py` | **Socket.IO 推送封装**（竞赛房 / PK 房 / 个人房） |
| `competition/socket_events.py` | Socket.IO 房间事件注册 |

#### 2. Socket.IO 实时通信接入

- **Flask-SocketIO 5.x**，`async_mode='threading'`（无需 Redis 支持），自动降级轮询
- **3 类房间**：

| 房间名 | 触发事件 | 推送内容 |
|---|---|---|
| `competition_{id}` | `join_competition` / `leave_competition` | 竞赛榜单 Top10（`leaderboard_update`） |
| `battle_{id}` | `join_battle` / `leave_battle` | PK 对战进度/得分/结果（`battle_update` / `battle_end` / `battle_accepted`） |
| `user_{id}` | `join_user`（需 JWT 鉴权） / `leave_user` | 个人段位/积分/勋章变化（`profile_update` / `badge_granted`） |

- **推送触发点**：交卷/超时结算后推榜单，PK 结算后推段位 + 勋章
- **前端兜底**：榜单 30 秒轮询兜底，Socket.IO 断连自动重连

#### 3. 段位 & 积分系统

- **6 档段位**：青铜(0) → 白银(100) → 黄金(250) → 铂金(450) → 钻石(700) → 王者(1000)
- **PK 积分**：胜 25 + 连胜加成（每连胜 +2，封顶 +10）/ 平 10 / 负 5
- **限时赛积分**：参与 5 + 表现分 ≤ 30（按得分率） + 名次分（仅多人参赛） + 满分加成 10
- **段位实时推导**：由积分实时计算（不入库），无漂移风险
- **段位色值**：青铜 `#b08d57` / 白银 `#a8b4c4` / 黄金 `#f0c14b` / 铂金 `#7fd8d8` / 钻石 `#7db9f5` / 王者 `#ff7a45`

#### 4. 赛季系统

- **自然月赛季**（YYYY-MM），每月 1 号自动开启新赛季
- **惰性归档**：访问时检测过期赛季，`season_meta` CAS 抢锁保证并发安全
  - `UPDATE season_meta SET archived=1 WHERE season=:s AND archived=0` 抢锁
  - 唯一约束 + 事务回滚保证幂等
- **归档快照**：`season_archive` 表固化排名 / 积分 / 段位 / 战绩
- **Top3 勋章**：归档时自动为前三名发放「季度巅峰」勋章
- **新赛季重置**：自动创建 `season_meta` 记录，积分档案初始化

#### 5. 勋章系统（7 种）

| 勋章 code | 名称 | 触发条件 |
|---|---|---|
| `first_pk_win` | 首战告捷 | 首次在 1v1 PK 中获胜 |
| `pk_streak_3` | 三连胜 | PK 达成 3 连胜 |
| `pk_streak_5` | 五连王者 | PK 达成 5 连胜 |
| `timed_finish_10` | 身经百战 | 限时赛累计完赛 10 场 |
| `perfect_score` | 一战封神 | 限时赛取得单场满分 |
| `season_top3` | 季度巅峰 | 赛季结算时全校排名前三 |
| `team_found` | 开疆辟土 | 创建了自己的战队 |

- **事件驱动**：PK 结算 / 限时赛结算 / 建战队时自动校验发放
- **幂等发放**：同一勋章 + 赛季不重复授予
- **实时推送**：勋章发放后通过 Socket.IO 推送到个人房间，前端弹出 Toast

#### 6. 战队系统

- **固定战队，一人一队**（防止多战队刷分）
- **完整 CRUD**：创建 / 加入 / 退出 / 转让队长 / 解散
- **实时积分**：战队积分 = 队员当前赛季积分之和（不入库，实时聚合）
- **战队排行**：按总积分排序
- **权限隔离**：仅学生可操作战队

#### 7. 学情画像（五维雷达图）

| 维度 | 计算公式 |
|---|---|
| 准确率 | 考试 + 竞赛总答对题数 / 总答题数 |
| 速度 | 考试平均用时 / 限时赛用时基准 |
| 竞技力 | 当前赛季积分 / 10 |
| 稳定度 | 考试成绩方差的归一化值 |
| 活跃度 | 近 30 天考试 + 竞赛次数 |

- **考试侧**：趋势图 + 题型正确率
- **竞赛侧**：段位 + PK 战绩 + 竞赛题型正确率 + 近期战绩
- **权限隔离**：学生看自己，教师可查所教学生

#### 8. 后端路由新增（5 个 Blueprint）

| Blueprint | URL 前缀 | 文件 | 功能 |
|---|---|---|---|
| `competition_bp` | `/api/competitions` | `competition/routes.py` | 竞赛管理（CRUD / 发布 / 题目 / 报名） |
| `competition_play_bp` | `/api/competitions` | `competition/play_routes.py` | 限时赛参赛 |
| `pk_bp` | `/api` | `competition/pk_routes.py` | 1v1 PK |
| `gamification_bp` | `/api` | `competition/gamification_routes.py` | 段位 / 赛季 / 勋章 / 学情画像 |
| `team_bp` | `/api` | `competition/team_routes.py` | 战队 |

#### 9. 前端新增（9 个视图 + socket.js）

| 文件 | 路由 | 功能 |
|---|---|---|
| `CompetitionPlaza.vue` | `/competitions` | 竞赛广场（学生浏览） |
| `CompetitionManage.vue` | `/competitions/manage` | 竞赛管理（教师 CRUD） |
| `CompetitionTake.vue` | `/competitions/:id/take` | 限时赛答题 |
| `CompetitionLeaderboard.vue` | `/competitions/:id/leaderboard` | Socket.IO 实时榜单 + 30s 轮询兜底 |
| `PkLobby.vue` | `/pk/lobby` | PK 大厅 |
| `PkTake.vue` | `/pk/battle/:id` | PK 答题 |
| `RankCenter.vue` | `/rank` | 段位中心（段位卡片 + 赛季榜 + 勋章墙） |
| `TeamCenter.vue` | `/teams` | 战队中心 |
| `StudyProfile.vue` | `/study-profile` | 学情画像（五维雷达图） |
| `utils/socket.js` | — | **Socket.IO 客户端单例** + `joinUserRoom` / `leaveUserRoom` |

#### 10. 侧边栏入口 & 实时推送监听

- **Layout.vue 侧边栏新增**：我的段位 / 战队 / 学情画像
- **个人房间监听**：登录后自动 `joinUserRoom`，监听 `profile_update` / `badge_granted`，弹出 Element Plus `ElNotification` Toast

#### 11. 修复的 Bug

| 问题 | 根因 | 修复 |
|---|---|---|
| Portal 学生端竞赛卡片点击无响应 | `disabled: true` 占位标记未移除 | 移除 disabled + 添加 `path: '/competitions'` |
| Socket.IO 连接失败（Redis） | 无 Redis 运行导致启动报错 | `async_mode='threading'`，无需 Redis |
| 限时赛单人参赛得名次分 | 单人竞赛不应获得名次分 | 添加 `multiplayer = len(board) >= 2` 判断 |
| PK 计分 autoflush 重复 | `sum(participant.answers) + gained` 触发 autoflush 导致当前题重复计分 | commit 后 SQL 重新聚合 |
| 排行榜只显示 FINISHED | 实时榜单失去意义 | 改为包含 `status != JOINED` 用户 |
| Swords 图标不存在 | `@element-plus/icons-vue` 无此图标 | 替换为 Lightning |
| 创建战队勋章不发放 | `check_and_grant` elif 链缺 `team_found` 分支 | 补上事件驱动分支 |
| 赛季归档不触发 | 测试只造了积分档案没造 `season_meta` 行 | 测试补造元信息行 |

#### 12. 依赖新增

```powershell
pip install Flask-SocketIO python-socketio simple-websocket
```

同时创建 `requirements.txt` 覆盖全部 Python 依赖（精确版本号）。

#### 13. 测试验证

- 端到端测试 **38/38 通过**，覆盖限时赛积分 / PK 连胜 / 勋章发放 / 战队管理 / 赛季归档 / 学情画像 / 权限隔离
- 测试脚本与临时数据已清理，用户库只保留 nfpz / 123 两个真实账号

### 修改的文件清单

| 文件路径 | 修改内容 |
|----------|----------|
| `competition/models.py` | **新建**：竞赛/PK/段位/赛季/勋章/战队 11 张表 |
| `competition/routes.py` | **新建**：竞赛管理 API |
| `competition/play_routes.py` | **新建**：限时赛参赛 API |
| `competition/pk_routes.py` | **新建**：1v1 PK API |
| `competition/gamification.py` | **新建**：段位/积分/勋章结算服务层 |
| `competition/season.py` | **新建**：赛季惰性归档（CAS） |
| `competition/gamification_routes.py` | **新建**：段位/赛季/勋章/学情画像 API |
| `competition/team_routes.py` | **新建**：战队 API |
| `competition/scoring.py` | **新建**：竞赛计分 |
| `competition/leaderboard.py` | **新建**：榜单聚合 |
| `competition/realtime.py` | **新建**：Socket.IO 推送封装 |
| `competition/socket_events.py` | **新建**：Socket.IO 房间事件注册 |
| `app.py` | 注册 5 个新 Blueprint + Socket.IO 初始化 + `points_settled` 自动迁移 |
| `frontend/src/utils/socket.js` | **新建**：Socket.IO 客户端单例 |
| `frontend/src/views/Portal.vue` | 移除学生端竞赛卡片 `disabled: true` |
| `frontend/src/views/Layout.vue` | 侧边栏新增段位/战队/学情画像入口 + Socket.IO 个人房间监听 |
| `frontend/src/views/RankCenter.vue` | **新建**：段位中心 |
| `frontend/src/views/TeamCenter.vue` | **新建**：战队中心 |
| `frontend/src/views/StudyProfile.vue` | **新建**：学情画像 |
| `frontend/src/views/CompetitionPlaza.vue` | **新建**：竞赛广场 |
| `frontend/src/views/CompetitionManage.vue` | **新建**：竞赛管理 |
| `frontend/src/views/CompetitionTake.vue` | **新建**：限时赛答题 |
| `frontend/src/views/CompetitionLeaderboard.vue` | **新建**：实时榜单 |
| `frontend/src/views/PkLobby.vue` | **新建**：PK 大厅 |
| `frontend/src/views/PkTake.vue` | **新建**：PK 答题 |
| `frontend/src/router/index.js` | 新增 9 条竞赛路由 |
| `说明文档.md` | 全面更新至 v3.0（蓝图 16→21、数据库表 13→25、新增 Socket.IO/竞技化章节） |
| `requirements.txt` | **新建**：精确版本号 Python 依赖清单 |

---

## 2026-09-07

### 一、时区统一改造（aware datetime 全链路）

针对系统历史上混用本地时间与 UTC 时间（`datetime.now()` 与 `datetime.utcnow()` 并存、SQLite 裸写日期 `T` 分隔符导致字符串比较失效）的问题，做一次性数据迁移并彻底切到 aware datetime。

- **统一约定**：
  - 数据库层：一律存 naive UTC（`UTCDateTime` 类型在读写时自动转换）
  - Python 层：一律 aware UTC（`datetime.now(timezone.utc)`）
  - 展示/输入层：一律 Asia/Shanghai 墙上时间（中国无夏令时，固定 UTC+8，不引入 pytz/tzdata）
- **新增 `utils/timeutil.py`**：
  - `utcnow()` — 全系统唯一的「现在」来源（aware UTC）
  - `localtime()` / `fmt_local()` — 转 Asia/Shanghai
  - `iso_local()` — 序列化成本地 ISO 字符串（前端按本地解析）
  - `parse_dt()` — 用户输入转 aware UTC（无时区按 +8 解释，带 `Z` 按其 时区转）
  - `season_of()` — 赛季归属按本地日历月切（不能按 UTC 月）
- **新增 `database/types.py` 的 `UTCDateTime`（TypeDecorator）**：
  - `process_bind_param`：aware → naive UTC 落库
  - `process_result_value`：naive → 补 aware UTC
  - `database/models.py` 与 `competition/models.py` 中全部 `db.DateTime` → `UTCDateTime`，`default=datetime.utcnow` → `default=utcnow`
- **Alembic 一次性数据迁移 `0002_tz_normalize`**：
  - 识别 UTC 列（`default=utcnow` 写入，无需处理）与本地列（`datetime.now` 写入，-8h 转换）
  - 混合列（`result.submitted_at` / `exam_session.end_time`）按行级判别转换
  - 用哨兵表 `tz_migration_v1` 防止重跑
- **展示层**：Jinja 加 `localtime` 过滤器；API 序列化统一 `iso_local()`；输入解析统一 `parse_dt()`

### 二、引入 Flask-Migrate / Alembic 迁移体系

此前表结构靠 `db.create_all()` + `init_database()` 里硬编码 `ALTER TABLE` 维护，每次启动都改表。现引入正式迁移工具：

- **`requirements.txt` 新增** `Flask-Migrate==4.1.0`
- **`app.py` 集成**：`migrate = Migrate(app, db)`，启动时 `db.create_all()`（新库）后 `flask_migrate.upgrade()`（幂等，已到最新版本则跳过）
- **迁移链**：

| 版本 | 文件 | 内容 |
|---|---|---|
| `0001_baseline` | `0001_baseline.py` | 基线锚点（表结构仍由 create_all 管理） |
| `0002_tz_normalize` | `0002_tz_normalize.py` | 历史数据时区归一化（哨兵表防重跑） |
| `0003_schema_refactor` | `0003_schema_refactor.py` | 补全 10 个历史缺失列（幂等 ADD COLUMN）+ Result 唯一索引 |

### 三、四项重构

**1. Result 加唯一约束，防止并发重复记录**
- `Result` 模型加 `UniqueConstraint('exam_id', 'student_id', name='_result_exam_student_uc')`
- `0003` 迁移用 `CREATE UNIQUE INDEX IF NOT EXISTS idx_result_exam_student` 建约束（SQLite 无需重建表）
- 两个写入点（`exam_execution/routes.py` 提交交卷、`app.py` 过期自动收卷）加 `try/except IntegrityError`，并发冲突时回退查询已有记录

**2. 定时任务从 before_request 剥离**
- 原 `auto_end_expired_exams` 挂在 `before_request`，每个请求都查 DB，且模块级 `_last_auto_end_time` 标志在多线程/多进程下不可靠
- **新增 `utils/scheduler.py`**：单后台 daemon 线程，固定 60s 周期执行 `auto_end_expired_exams()` + `settle_pending_competitions()` + `ensure_seasons()`
- `before_request` 只保留访问日志；请求路径不再做全表结算
- 竞赛 `play_routes`/`pk_routes` 中针对单场竞赛的 `sync_status()`（用户进入时必要）保留

**3. 修复 User 的 bcrypt 循环导入**
- 原 `User.set_password/check_password` 方法内 `from app import bcrypt`，形成 models → app 反向依赖
- `bcrypt = Bcrypt()` 移到 `database/__init__.py`，`app.py` 改 `bcrypt.init_app(app)`
- `models.py` 改为 `from database import bcrypt`，切断循环依赖

**4. init_database 迁移脚本化**
- 原 `init_database()` 中 10 个硬编码 `ALTER TABLE ADD COLUMN` 迁入 `0003_schema_refactor`（幂等：列已存在则跳过）
- `init_database()` 简化为 `create_all()` + `upgrade()` + 幂等数据修正（题型/难度小写、课程学分），启动不再反复改表

### 四、后续健壮性优化

- **清理空 `_gate()`**：结算迁移到后台线程后，`gamification_routes.py` / `team_routes.py` 中 14 处 `_gate()` 调用、两个空函数定义及未使用的 `settle_pending_competitions`/`ensure_seasons` 导入全部移除
- **scheduler 任务隔离**：三个周期任务各自独立 `try/except`，单点失败不阻塞其余任务，异常时主动 `db.session.rollback()`
- **`ensure_profile` 并发兜底**：`UserPointsProfile` 唯一约束 `(user_id, season)` 上，并发创建捕获 `IntegrityError` 后回退查询
- **`_grant` 勋章发放并发兜底**：`UserBadge` 唯一约束 `(user_id, badge_code, season)` 上，并发重复发放捕获 `IntegrityError` 视为已存在

### 五、代码审查报告问题修复

| 问题 | 修复 |
|---|---|
| 日志文件并发写入冲突（Windows `WinError 32`） | `utils/logger.py` 移除 root logger 上重复的 access/jinja2 handler（与独立 logger 重复打开同一文件）；`TimedRotatingFileHandler` → `RotatingFileHandler`（按 10MB 大小轮转，避开午夜 rename 冲突） |
| CSRF 豁免范围过大（所有 `/api/*` 蓝图统一 exempt） | `utils/security.py` 关闭默认检查，改 `_smart_csrf_check`：带 `Authorization` header 的 Bearer 请求自动豁免；公开接口白名单（`/api/login`、`/api/register`、`/api/csrf-token`、`/api/logs`）豁免；其余强制 CSRF 校验 |
| `Question.options`/`CompetitionQuestion.options` 用 Text 存 JSON | `database/types.py` 新增 `JSONType`（底层 TEXT，写入 dict→dumps、读取→loads，兼容历史字符串），两处列类型替换，移除 3 处手动 `json.loads` |
| `config.py` 残留注释代码 | 删除注释掉的旧 `DATABASE_URL`/`DOUBAN_API` 配置行 |
| `__pycache__` 旧版本 .pyc 污染 | 清理所有模块的 `__pycache__` 目录 |

### 验证

- 迁移链执行至 `0003_schema_refactor`，哨兵表与唯一索引均落库
- 重复 `(exam_id, student_id)` 插入被 `IntegrityError` 拒绝；`ensure_profile`/`_grant` 幂等
- CSRF：Bearer 请求 200、登录接口 200、非豁免 POST 无 Authorization 返回 400
- 所有核心 API（exams/rank/teams/profile/badges）返回 200，启动日志无 `PermissionError`，scheduler 线程正常启动

### 修改 / 新增的文件

| 文件路径 | 修改内容 |
|----------|----------|
| `utils/timeutil.py` | **新建**：时区统一工具（utcnow/localtime/iso_local/parse_dt/season_of） |
| `utils/scheduler.py` | **新建**：后台定时调度 daemon 线程 |
| `database/types.py` | **新建**：`UTCDateTime` + `JSONType` 自定义列类型 |
| `database/__init__.py` | 新增 `bcrypt = Bcrypt()`，消除循环导入 |
| `database/models.py` | DateTime→UTCDateTime、options→JSONType、Result 唯一约束 |
| `competition/models.py` | DateTime→UTCDateTime、CompetitionQuestion.options→JSONType |
| `app.py` | 集成 Migrate + 启动 upgrade、scheduler 启动、auto_end 移出 before_request、bcrypt.init_app、移除统一 csrf.exempt、Result IntegrityError 兜底、init_database 简化 |
| `utils/security.py` | CSRF 智能检查（Authorization 豁免 + 白名单 + 强制校验） |
| `utils/logger.py` | RotatingFileHandler + 去除重复 handler |
| `config.py` | 清理注释掉的旧配置 |
| `competition/gamification.py` | ensure_profile / _grant 加 IntegrityError 兜底 |
| `competition/gamification_routes.py` | 移除空 `_gate()` 及未用导入 |
| `competition/team_routes.py` | 移除空 `_gate()` 及未用导入 |
| `exam_execution/routes.py` | Result 提交 IntegrityError 兜底、options 去 json.loads |
| `result/routes.py` | options 去 json.loads |
| `migrations/versions/0001_baseline.py` | **新建**：迁移基线 |
| `migrations/versions/0002_tz_normalize.py` | **新建**：历史数据时区归一化 |
| `migrations/versions/0003_schema_refactor.py` | **新建**：缺失列补全 + Result 唯一索引 |
| `requirements.txt` | 新增 Flask-Migrate==4.1.0 |

---

## 2026-09-07（续）— 代码审查问题修复（严重 + 中等 + 轻微）

对全项目做系统性代码审查，发现并修复 19 个问题，分三批完成，每批均配 E2E 测试验证。

### 一、严重问题修复（3 项，E2E 10/10 通过）

| # | 问题 | 修复 | 关键文件 |
|---|---|---|---|
| ① | **抽题绕过**：api_answer 不校验题目是否在抽题固化范围内，学生可答未分配题 | `play_routes.api_answer` 校验 `assigned_cq_ids` 范围，越界返回 403 | `competition/play_routes.py` |
| ② | **PK 积分丢失**：`_settle_battle` lobby 路径积分发放后无 commit，被后续 rollback 吞掉 | `award_pk_result` 后内部 `db.session.commit()` | `competition/pk_routes.py` |
| ③ | **PK 并发双发积分**：两个请求同时结算同一 battle，各发一次积分 | CAS 抢结算权：`UPDATE pk_battle SET status='finished' WHERE id=? AND status='playing'`，affected_rows=0 则放弃 | `competition/pk_routes.py` |

**附带修复**：api_pk_answer 结算被抢后拒绝继续作答；api_pk_finish 按实际状态返回而非报错。

### 二、中等问题修复（6 项，E2E 13/13 通过）

| # | 问题 | 修复 | 关键文件 |
|---|---|---|---|
| ④ | **Socket.IO 房间无鉴权**：任何人可 join 任意竞赛/PK 房间 | `join_competition` 需 JWT；`join_battle` 仅限 challenger/opponent；前端两页面已传 token 并重建 dist | `competition/socket_events.py`、`frontend/src/utils/socket.js` |
| ⑤ | **切屏防作弊仅前端**：3 次切屏由前端判断提交，可绕过 | `report_switch` 服务端计数，达 3 次调 `_finalize_submission` 本地判分强制交卷（不等 AI） | `exam_execution/routes.py` |
| ⑥ | **考试执行页无会话校验**：take_exam/submit_exam 不校验会话存在性/归属/跨考试 | 加入 `current_session_id` 存在性、`student_id` 归属、`exam_id` 匹配校验；submit 改 POST-only（GET 405） | `exam_execution/routes.py` |
| ⑦ | **time_spent 客户端可篡改**：速度分依赖客户端传值 | `play_routes.api_answer` + `pk_routes.api_pk_answer` 由 `answered_at` 差值计算 time_spent（int 截断，<1s 计 0），客户端传值被忽略 | `competition/play_routes.py`、`competition/pk_routes.py` |
| ⑧ | **CSRF 验证失败**：`WTF_CSRF_SECRET_KEY` 显式 None 不触发 Flask-WTF 回落 | config.py 用 `or` 兜底：`WTF_CSRF_SECRET_KEY = os.getenv(...) or (os.getenv('SECRET_KEY') or 默认值)` | `config.py` |
| ⑨ | **被考试引用的题目可删除**：删后考试数据断裂 | 页面路由 + API 双守卫：检查 `ExamQuestion` 引用，有则拒绝并提示 | `question_bank/routes.py`、`api/question_routes.py` |

### 三、轻微问题修复（10 项，E2E 15/15 通过）

| # | 问题 | 修复 | 关键文件 |
|---|---|---|---|
| ⑩ | **verify_token 12 处重复实现**：每个 API 文件各自定义 | 统一抽取到 `utils/security.py`，12 个文件改 `from utils.security import verify_token` | `utils/security.py` + api/ + competition/ 共 13 文件 |
| ⑪ | **遗留诊断脚本**：根目录 `_check_db.py` | 删除 | `_check_db.py` |
| ⑫ | **AI count 参数无容错**：非数字直接 500 | try/except int 解析 + clamp [1, 20] | `api/ai_routes.py` |
| ⑬ | **options 存储格式不一致**：AI 路径存 JSON 数组，手工表单路径存空（表单发 option_A..H 但后端读 options 字段不存在） | 新增 `_collect_form_options`（表单路径收集 option_A..H）+ `_normalize_options`（API 路径归一），统一存 JSON 数组；顺带补 `api/question_routes.py` 缺失的 `import json` | `question_bank/routes.py`、`api/question_routes.py` |
| ⑭ | **AI 评分未 clamp**：AI 返回异常值导致成绩溢出 | AI 返回百分比 clamp [0, 100] 再换算满分 | `ai_service/services.py` |
| ⑮ | **lobby N+1 查询**：循环内 `len(c.questions)`/`len(c.participants)`/`get_my_rank()` 各触发查询 | 题目数/参与人数改批量聚合 `GROUP BY`；`get_my_rank(participant=)` 复用已查行；leaderboard 加 `joinedload(user)` | `competition/play_routes.py`、`competition/leaderboard.py` |
| ⑯ | **WAITING 挑战永不过期**：无人接受的挑战永久阻塞发起人 + 出现在大厅 | 30 分钟自动置 `cancelled`（`_expire_stale_waiting`，create/lobby/accept 三入口调用）；`_is_cancelled` 不进 mine 列表；lobby 对已删竞赛 `b.competition None` 加守卫 | `competition/pk_routes.py` |
| ⑰ | **gamification 字符串硬编码**：`'draft'` 字符串不安全 | 改用 `CompetitionStatusEnum.DRAFT` 枚举常量 | `competition/gamification.py` |
| ⑱ | **updated_at 不自动更新**：`UserPointsProfile.updated_at` 无 onupdate | 加 `onupdate=utcnow` | `competition/models.py` |
| ⑲ | **错误处理器不区分 API/页面**：所有错误都返回 JSON；`inject_notifications` 对 API 请求也查通知 | `_wants_json()` 区分 → API 返回 JSON，浏览器返回内联 HTML 错误页；`inject_notifications` 对 `/api/*` 直接跳过 | `app.py` |

**附带发现**：库中一条泄漏的孤儿测试数据（pk_battle id=1，status 大写 'WAITING'，引用已删除的竞赛 id=1），已清理。

### 验证

- **严重问题 E2E**（10/10 通过，临时双账号 e2e_p1/p2_*，测完清理）：覆盖抽题校验、积分发放、CAS 并发结算
- **中等问题 E2E**（13/13 通过，临时账号 e2e_m_*，测完清理）：覆盖 Socket.IO 鉴权、切屏强制交卷、会话校验、time_spent 服务端计算、CSRF 兜底、题目删除守卫
- **轻微问题 E2E**（15/15 通过，临时脚本测完删除）：覆盖 verify_token 统一后各模块 JWT 认证、options 往返、lobby 字段完整、挑战过期作废、404 API/页面区分、clamp 表达式

### 修改的文件

| 文件路径 | 修改内容 |
|----------|----------|
| `utils/security.py` | 新增统一 `verify_token()` 函数 |
| `api/student_routes.py` | 删除本地 verify_token，改 import |
| `api/result_routes.py` | 删除本地 verify_token，改 import |
| `api/execution_routes.py` | 删除本地 verify_token，改 import |
| `api/exam_routes.py` | 删除本地 verify_token，改 import |
| `api/chapter_routes.py` | 删除本地 verify_token，改 import |
| `api/auth_routes.py` | 删除本地 verify_token，改 import |
| `api/ai_routes.py` | 删除本地 verify_token，改 import + count 参数容错 |
| `api/question_routes.py` | 删除本地 verify_token + 新增 `_normalize_options` + 补 `import json` |
| `competition/team_routes.py` | 删除本地 verify_token，改 import |
| `competition/routes.py` | 删除本地 verify_token，改 import |
| `competition/play_routes.py` | 删除本地 verify_token + lobby N+1 批量聚合 + get_my_rank 复用行 |
| `competition/pk_routes.py` | 删除本地 verify_token + WAITING 过期清理 + CAS 结算 + 积分 commit |
| `competition/gamification_routes.py` | 删除本地 verify_token，改 import |
| `competition/leaderboard.py` | get_my_rank(participant=) + joinedload(user) |
| `competition/gamification.py` | `'draft'` → CompetitionStatusEnum.DRAFT |
| `competition/models.py` | PkBattleStatusEnum 新增 CANCELLED + updated_at onupdate |
| `competition/socket_events.py` | 房间 JWT 鉴权（join_competition / join_battle） |
| `exam_execution/routes.py` | _finalize_submission + 会话校验 + 切屏强制交卷 + submit POST-only |
| `competition/play_routes.py` | api_answer 校验 assigned_cq_ids + time_spent 服务端计算 |
| `ai_service/services.py` | AI 评分 clamp [0,100] |
| `config.py` | SECRET_KEY / WTF_CSRF_SECRET_KEY 用 or 兜底 |
| `question_bank/routes.py` | _collect_form_options + 题目删除守卫 |
| `app.py` | 错误处理器区分 API/页面 + inject_notifications 跳过 API |
| `frontend/src/utils/socket.js` | Socket.IO 连接传 JWT token |
| `_check_db.py` | **删除** |
| `tools/check_db.py` | **删除**（tools/ 目录整体删除） |
| `tools/check_data.py` | **删除** |
| `tools/test_ai.py` | **删除** |

---

## 2026-09-08

### 一、新增征集悬赏模块（`bounty/` 包）

继考试、竞赛之后的第三大业务模块：师生均可发布**题目征集**或**答案征集**悬赏，其他用户投稿、发布者审核采纳，采纳后题目自动入题库 / 答案解析自动回填，并向投稿人发放竞技积分与勋章。模块完全独立，对竞赛核心代码零侵入（`app.py` 仅增 1 行 import + 1 行蓝图注册）。

#### 1. 后端新增（4 个文件）

| 文件 | 功能 |
|------|------|
| `bounty/__init__.py` | 包标识 |
| `bounty/models.py` | 2 张表：`Bounty` 悬赏单（16 字段）、`BountySubmission` 投稿（17 字段）；联合唯一约束 `_bounty_submitter_uc(bounty_id, submitter_id)` 防重复投稿 |
| `bounty/routes.py` | 9 个 JWT 接口（Blueprint `bounty`，统一 `/api` 前缀）：广场/发布/详情/投稿/我的/采纳/拒绝 |
| `bounty/rewards.py` | 奖励结算：`award_bounty()` 发竞技积分 + 「悬赏新星🪙」勋章（`bounty_first_accept`），复用 `competition.gamification` 的段位/赛季能力与 `competition.realtime` 的 Socket.IO 推送 |

#### 2. 两种悬赏类型

| 类型 | `bounty_type` | 投稿内容 | 采纳结果 |
|---|---|---|---|
| 题目征集 | `question` | 自编题：题干/选项/答案/解析/题型/难度/知识点 | 自动创建 `Question(source='bounty')` 入题库，`accepted_question_id` 回填 |
| 答案征集 | `answer` | 答案/解析正文 | 若关联题库题（`target_question_id`）且原题 `analysis` 为空则自动回填解析；也可自带题目快照（`target_question_snapshot` JSON）不依赖题库 |

- 悬赏单字段：`reward_points`（赏金，默认 10，正整数校验）、`deadline`（截止时间，可空=长期有效）、`status`（open/closed；`expired` 不落库，由 `effective_status()` 按 deadline 惰性反映，过期不可投稿）
- 题目悬赏发布时 `major_id` 必填（采纳入库归属专业）；题型/难度投稿缺省时继承悬赏单设置

#### 3. 9 个 API 接口

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/bounties` | 悬赏广场（分页/类型/状态筛选，含发布者名、专业名、投稿数、待审核数、我的投稿） |
| POST | `/api/bounties` | 发布悬赏（教师/学生均可） |
| GET | `/api/bounties/mine` | 我发布的悬赏 |
| GET | `/api/bounties/my-submissions` | 我的投稿 |
| GET | `/api/bounties/<id>` | 悬赏详情 |
| GET | `/api/bounties/<id>/submissions` | 投稿列表（仅发布者可见全部，投稿人只见自己的） |
| POST | `/api/bounties/<id>/submissions` | 提交投稿（每人每悬赏限 1 条，IntegrityError 兜底 400） |
| POST | `/api/submissions/<sid>/accept` | 采纳投稿（仅发布者） |
| POST | `/api/submissions/<sid>/reject` | 拒绝投稿（可附评语，仅发布者） |

#### 4. 采纳闭环与并发安全

采纳（`submission_accept`）在单事务内完成 6 步：

1. **CAS 抢锁**：裸 SQL `UPDATE bounty SET status='closed', closed_at=?, accepted_submission_id=? WHERE id=? AND status='open'`，rowcount≠1 即视为已被采纳，直接返回（防并发重复采纳/重复发奖）
2. 投稿置 `accepted`、写 `reviewer_id` / `reviewed_at`
3. 题目投稿 → 创建 `Question(source='bounty')` 入库并回填 `accepted_question_id`；答案投稿 → 原题无解析时回填 `analysis`
4. 同悬赏其余 `pending` 投稿批量置 `rejected`，评语自动写"悬赏已采纳其他投稿"
5. commit 后调用 `award_bounty()`：发竞技积分 + 「悬赏新星🪙」勋章，独立 try/except——奖励失败仅记日志，**不回滚采纳结果**（业务优先）
6. Socket.IO 推送 `profile_update`（reason='bounty'）+ `badge_granted` 到投稿人个人房间

- 奖励复用竞赛竞技化能力：`ensure_profile`/`tier_of`/`current_season`，积分计入当前自然月赛季与段位；`IntegrityError` 时回滚勋章只保留积分
- 拒绝：投稿置 `rejected` + `review_comment` + `reviewer_id`

#### 5. 权限规则

- 发布/投稿/采纳：教师与学生**同权**（JWT 登录即可）
- 禁止给自己发布的悬赏投稿（400）
- 采纳/拒绝仅悬赏发布者可操作（403）
- 投稿列表仅发布者可见全部，其他人只能看到自己的投稿
- 认证统一使用 `utils/security.py` 的 `verify_token()`（初版曾在模块内自定义，同日已收敛，见第四节）

#### 6. 前端新增（4 个视图）

| 文件 | 路由 | 功能 |
|---|---|---|
| `BountyPlaza.vue` | `/bounties` | 悬赏大厅：卡片列表、类型/状态筛选、倒计时、投稿数、我是否已投 |
| `BountyPublish.vue` | `/bounties/publish` | 发布悬赏：题目征集（专业/题型/难度）与答案征集（关联题库题/自带快照）两种表单 |
| `BountyDetail.vue` | `/bounties/:id` | 详情页：发布者看投稿列表+采纳/拒绝按钮；投稿人看投稿表单+自己的投稿状态 |
| `BountyMine.vue` | `/bounties/mine` | 我的悬赏（发布的 + 投稿的两个页签，含审核状态） |

- `router/index.js` 新增 4 条路由（`meta.module='bounty'`）
- `Layout.vue` 侧边栏新增「悬赏大厅 / 发布悬赏 / 我的悬赏」3 个入口（师生均可见）
- `Portal.vue` 教师端与学生端均新增「征集悬赏」功能卡片

#### 7. 验证

- 端到端测试 **21/21 通过**：发布两种悬赏 → 投稿（含重复投稿拦截/自我投稿拦截）→ 采纳（题目入库 `source='bounty'`、解析回填、其余投稿自动拒绝）→ 积分+勋章到账 → Socket.IO 推送 → 过期悬赏不可投稿 → 权限隔离
- 临时测试账号/脚本测完已清理，根目录无残留

### 二、日志系统并发冲突修复

针对 Windows 环境下日志轮转与高频请求导致的一系列问题：

| 问题 | 根因 | 修复 |
|---|---|---|
| 日志轮转时服务崩溃（`WinError 32` / PermissionError） | `RotatingFileHandler.doRollover` 在 Windows 上 rename 被其他线程/进程占用的日志文件时抛异常，冒泡导致请求 500 | `utils/logger.py` 新增 `SafeRotatingFileHandler`：轮转 rename 失败时捕获异常、不轮转、重开原文件继续写入，不崩不丢日志 |
| access.log 暴涨自激循环 | 前端日志上报 `POST /api/logs/vue` 本身被访问日志记录 → 触发前端再上报 → 无限放大；`/socket.io` 轮询握手每秒数次 | `app.py` 新增 `_LOG_SILENT_PREFIXES`（`/api/logs`、`/socket.io`、`/api/csrf-token`、`/static/`、`/assets/`），访问日志钩子对这些路径静默 |
| werkzeug 请求日志刷屏 | werkzeug logger 默认 INFO，每个 HTTP 请求一行（与 access logger 重复审计） | werkzeug logger 提升至 **WARNING**，请求审计统一由 access logger 负责 |
| 应用内执行迁移后日志停写 | Alembic `migrations/env.py` 无条件 `fileConfig()`，默认 `disable_existing_loggers=True` 且重置 root handlers，导致 root 文件 handler 被移除、access/jinja2 logger 被 disable | env.py 加守卫：`fileConfig` 仅在 root **尚无任何 handler**（脱离应用独立跑 alembic）时执行；Flask 应用内启动迁移时跳过，保留已配置的 app/error/access/jinja2 handler |
| logs 目录旧残留 | 历史 TimedRotating 产物（`.2026-09-xx` 等文件） | 已清理，logs/ 现仅保留 app.log / error.log / access.log / jinja2.log |

- 轮转策略：按大小 10MB、保留 30 份（09-07 已由 TimedRotating 切到 RotatingFileHandler，本次补安全降级）

### 三、说明文档更新至 v3.2

- 新增 **2.7 征集悬赏模块表结构详解**：bounty / bounty_submission 两张表逐字段说明（类型/约束/外键/业务含义）、关系图、状态流转图、CAS 幂等说明
- 新增 **4.10-4.14 API 章节**：竞赛管理（10 接口）、限时赛参赛（7 接口+防作弊 5 条）、1v1 PK（6 接口+CAS 结算）、竞技化（段位/赛季/勋章/战队/学情 13 接口）、征集悬赏（9 接口+采纳闭环）
- 新增 **6.6-6.8 业务流程**：限时赛 / PK / 悬赏 三套流程图；**7.6-7.10 关键机制**：速度加成计分、抽题防作弊、PK 惰性结算、悬赏采纳结算、日志轮转并发安全
- 修正历史文档错误：`CompetitionStatusEnum` 补 `ongoing` 四态、补全 `pk_battle`/`pk_answer`/`competition_answer` 字段、传统模板蓝图补 `chapter_bp`、竞赛表计数 11→12、视图 31→35、勋章 7→8、2.4 自动收卷机制改为后台调度线程、5.7 日志轮转描述更新

### 四、技术债清理：bounty 认证收敛 + 登录乱码修复

**1. bounty 模块 verify_token 收敛到统一版**

- 背景：bounty 初版在 `bounty/routes.py` 内自定义了 `verify_token()`（09-07 统一抽取时该模块尚未开发）
- 等价性核实：两版逐行对比完全一致——同为 `Authorization: Bearer` 解析、`jwt.decode(HS256, Config.SECRET_KEY)`、`db.session.get(User, payload['user_id'])`、失败返回 `None`；调用点 `if not user: 401` 模式也一致
- 循环导入排查：`utils/security.py` 顶部仅依赖 flask_limiter/flask_wtf/flask/config，db/User 函数内延迟 import，无业务模块依赖（competition 等 13 个文件早已用此模式）
- 改动：删除本地 12 行定义，顶部加 `from utils.security import verify_token`；顺带移除因此变为未使用的 `User` 导入；9 个调用点零改动
- 验证：`py_compile` 通过 + 导入身份断言（`bounty.routes.verify_token is utils.security.verify_token`）+ 冒烟测试（无 token 401 / 错误签名 401 / 有效 token 200）

**2. 修复登录失败消息乱码（既有 bug）**

- `api/auth_routes.py` 登录失败返回的 `'鐢ㄦ埛鍚嶆垨瀵嗙爜閿欒'` 是源文件内的乱码字面量（UTF-8 字节曾被按 GBK 误解码后保存），全项目扫描仅此 1 处
- 改回 `'用户名或密码错误'`，重启后端验证 401 响应消息显示正常

### 修改 / 新增的文件

| 文件路径 | 修改内容 |
|----------|----------|
| `bounty/__init__.py` | **新建**：包标识 |
| `bounty/models.py` | **新建**：Bounty + BountySubmission 两张表（含唯一约束、级联删除、effective_status 惰性过期） |
| `bounty/routes.py` | **新建**：9 个悬赏 API（发布/投稿/审核/列表/我的），CAS 采纳闭环；同日收敛为统一 `verify_token`（删除本地定义，9 调用点零改动） |
| `api/auth_routes.py` | 修复登录失败消息乱码字面量（`'鐢ㄦ埛鍚嶆垨瀵嗙爜閿欒'` → `'用户名或密码错误'`，既有 bug） |
| `bounty/rewards.py` | **新建**：积分+勋章结算，复用竞赛 gamification/realtime，IntegrityError 兜底 |
| `app.py` | 注册 `bounty_bp`（蓝图 26→27）+ 访问日志静默路径 `_LOG_SILENT_PREFIXES` |
| `utils/logger.py` | 新增 `SafeRotatingFileHandler`（Windows 轮转失败安全降级）+ werkzeug 提升 WARNING |
| `migrations/env.py` | `fileConfig` 加 root handler 守卫，防应用内迁移摧毁日志配置 |
| `frontend/src/views/BountyPlaza.vue` | **新建**：悬赏大厅 |
| `frontend/src/views/BountyPublish.vue` | **新建**：发布悬赏（题目/答案双表单） |
| `frontend/src/views/BountyDetail.vue` | **新建**：悬赏详情 + 投稿/审核 |
| `frontend/src/views/BountyMine.vue` | **新建**：我的悬赏与投稿 |
| `frontend/src/router/index.js` | 新增 4 条 bounty 路由 |
| `frontend/src/views/Layout.vue` | 侧边栏新增悬赏大厅/发布悬赏/我的悬赏入口（师生） |
| `frontend/src/views/Portal.vue` | 教师端+学生端新增「征集悬赏」功能卡片 |
| `说明文档.md` | 更新至 v3.2（悬赏模块 + 竞赛 API 详节 + 表结构 + 流程/机制章节 + 历史勘误） |
| `logs/` | 清理旧 TimedRotating 残留文件 |

---

## 2026-09-09 — 生产部署加固 / 性能与健壮性优化 / 说明文档 v3.3

> 背景：代码审查提出生产环境隐患（`debug=True` 硬编码、Werkzeug 开发服务器上生产、SECRET_KEY 开发默认值、Socket.IO CORS 通配符）与性能建议（过期收卷大事务、AI 代理全局副作用、启动全表修正、CORS 硬编码）。逐条对照源码核实后全部处理。

### 一、生产环境部署加固

**1. 调试开关环境变量化（`config.py` + `app.py`）**
- 问题：`app.py` 启动块两处 `debug=True` 硬编码，生产环境会启用 Werkzeug 调试器（可远程代码执行）
- 改动：`config.py` 新增 `DEBUG = os.getenv('FLASK_DEBUG', 'false').lower() == 'true'`（默认 **false**，非 `true` 一律视为 false）；`app.py` 的 `socketio.run` / `app.run` 改为 `debug=app.config['DEBUG']`
- 本地 `.env`（不进 Git）追加 `FLASK_DEBUG=true`，开发调试体验不变

**2. waitress 生产 WSGI 服务器（`app.py` + `requirements.txt`）**
- 问题：生产模式不应使用 Werkzeug 开发服务器（`allow_unsafe_werkzeug=True` 是明确的开发标记）
- 改动：`app.py` 启动块按 `Config.DEBUG` 分流——
  - 开发模式：沿用 `socketio.run(..., debug=True, allow_unsafe_werkzeug=True)`，Socket.IO 经 simple-websocket 支持原生 WebSocket
  - 生产模式：`waitress.serve(app, host, port, threads=16, channel_timeout=120)`，waitress 未安装时给出 `pip install waitress` 明确提示
- 原理：Flask-SocketIO 在 `init_app` 时已把 Socket.IO 中间件挂入 `app.wsgi_app`，waitress 直接 serve `app` 即可；waitress 不支持 WebSocket 升级，客户端**自动回退 HTTP 长轮询**（前端本有 30s 轮询兜底，PK/榜单功能不受影响）；Linux 如需原生 WebSocket 可用 gunicorn + eventlet/gevent
- `requirements.txt` 新增 `waitress==3.0.2`（纯 Python，Windows/Linux 通用，无 monkey-patching）

**3. SECRET_KEY 生产强制守卫（`config.py`）**
- 问题：SECRET_KEY 有兜底默认值 `dev-secret-key-change-in-production`，生产若不配置则 JWT 可被伪造、会话可被篡改
- 改动：生产模式（DEBUG=false）下 SECRET_KEY 缺失**或仍为开发默认值**时，`Config` 类加载即 `RuntimeError` 拒绝启动（fail-fast），错误信息附生成命令 `python -c "import secrets;print(secrets.token_hex(32))"`；开发模式保留兜底不影响本地
- 验证：4 场景单测全过（生产无密钥→拦截、生产默认密钥→拦截、生产强密钥→放行、开发无密钥→兜底放行）

**4. CORS 白名单环境变量化 + 按环境收窄（`config.py` + `app.py`）**
- 问题：HTTP CORS 列表硬编码在 `app.py`，且 Socket.IO 用 `cors_allowed_origins='*'`（任意网站可建立 Socket 连接）
- 改动：`config.py` 新增 `CORS_ORIGINS`——`CORS_ORIGINS` 环境变量（逗号分隔）显式配置优先；未配置时**开发模式**预置 localhost:5173-5176/127.0.0.1/5000/172.16.10.172 等本地源，**生产模式默认为空列表**（不带入 localhost 源；同源访问不受影响）。`app.py` 的 `CORS(app, origins=...)` 与 `SocketIO(cors_allowed_origins=...)` 统一使用该配置
- 验证：白名单内源（localhost:5173）回显 ACAO、白名单外源（evil.example.com）无 CORS 头、Socket.IO 轮询握手 200

**5. 监听地址配置**：新增 `FLASK_HOST`（默认 127.0.0.1，外网访问设 0.0.0.0）/ `FLASK_PORT`（默认 5000）

### 二、性能与健壮性优化

**1. 过期考试收卷：每考试独立事务 + 批量预载消除 N+1（`app.py` `auto_end_expired_exams()`）**
- 问题：所有过期考试共用一个大事务——SQLite 库级写锁在批量批改（逐答案查 ExamQuestion + 查 Question 的 N+1）期间被长时间持有，可能阻塞学生交卷；单个考试出错整批回滚、每轮反复失败
- 改动：
  - 每个考试独立 `try/commit/rollback`：单考失败只回滚自己，不影响其余考试，下轮自动重试
  - 开考时一次性批量预载本场考试的题目分值（`score_map`）与题目实体（`questions` 字典），消除逐答案 N+1 查询
  - `IntegrityError`（并发交卷导致成绩唯一约束冲突）整场回滚记 warning，下轮重试
  - 计分控制流（孤立题目 `if not question: continue`、多选排序比对/填空忽略大小写/判断大写比对规则）与原版**逐行核对保持一致**，仅数据来源换成预载字典
- 外层查询本身为选择性查询（`status=PUBLISHED AND end_time<now`，稳态 0 条），保持不变

**2. AI 请求代理改为请求级（`ai_service/client.py`）**
- 问题：模块级 6 行 `os.environ["HTTP_PROXY"]="" 等清空是进程级副作用，影响 websocket-client 等所有读环境变量的 HTTP 库
- 改动：删除模块级环境变量修改，改为 `requests.post(..., proxies={"http": None, "https": None})`，仅 AI 请求绕过系统代理（开发机代理软件拦截方舟请求超时的原问题同样解决）
- 验证：导入 client 后全局 `HTTP_PROXY/HTTPS_PROXY` 保持原值不变

**3. 启动数据修正改 SQL 端过滤（`app.py` `init_database()`）**
- 问题：每次启动遍历全部 Question 做大小写修正、遍历全部 Course 补学分，数据量大时启动慢
- 改动：Question 改为 `WHERE type != LOWER(type) OR difficulty != LOWER(difficulty)`、Course 改为 `WHERE credit IS NULL OR credit = 0`，只加载待修正行，稳态 0 行扫描；保留自愈能力（未来出现脏数据仍会修），无需迁移脚本

### 三、验证记录

- `py_compile` 三个改动文件全部通过
- 单测：SECRET 守卫 4 场景、CORS 3 场景（生产无配置→[]、生产显式域名→使用域名、开发→10 条本地源）、AI 客户端环境变量无副作用
- 生产模式实测（waitress，临时 5001 端口）：`/api/bounties` 无 token→401、`/socket.io/?EIO=4&transport=polling`→200 握手返回 sid、`/login`→200，日志 `waitress - Serving on http://127.0.0.1:5001` 且无 Debug mode
- 开发模式重启冒烟：Debug mode: on、鉴权 401、CORS 白名单内外行为正确、Socket.IO 200、首页 200
- 调度器首轮 `auto_end_expired_exams` 在新代码下运行无异常（Traceback/Error 检查为空）

### 四、文档

- `说明文档.md` 更新至 **v3.3**：安全配置章节重写环境变量清单与生产建议表、技术栈新增 waitress、3.2 启动流程补第 11 步启动模式分流、3.3 AI 代理描述更新、后台调度线程补事务粒度说明、8.1 依赖/环境变量模板更新、8.5 生产部署改为 waitress 为主（gunicorn 降为 Linux 备选）、FAQ 9.1/9.2/9.3 同步、追加 v3.3 更新记录

### 五、补记：v3.2 代码推送 GitHub（09-08 晚，当日漏记）

- `git pull` 报 `fatal: refusing to merge unrelated histories`：远端仓库曾被网页重新上传（`forced update`），远端为 v2.8 旧快照（无 competition/bounty/migrations，233 文件），与本地 v3.2（428 文件）无共同祖先
- 确认同步方向应为**本地→远端**（远端更旧），未做 merge
- 首次推送被 **GitHub GH013 Push Protection 拦截**：本地旧初始提交 `810766a` 中的 `.env` 含火山引擎 Ark API Key；确认该 Key **从未上传过 GitHub**（远端旧快照无 .env）
- 处理：孤儿分支重建干净单提交历史（`c6291e2`，266 文件），历史中不含任何带密钥提交；`git reflog expire + gc --prune=now` 清除本地含密钥旧对象；`.gitignore` 补 `.trae/`、`logs/`、`frontend/dist/`，并清除历史误入的 `*.pyc`、`.idea`
- `git push --force-with-lease` 成功，仓库 https://github.com/Gmsfzy/exam-system-Beta 的 main 已为 v3.2 最新代码，本地 main 与 origin/main 同步
- 安全建议（可选）：到火山引擎控制台轮换一次 Ark API Key

### 修改 / 新增的文件

| 文件路径 | 修改内容 |
|----------|----------|
| `config.py` | 新增 `DEBUG`（FLASK_DEBUG）、`HOST`/`PORT`（FLASK_HOST/FLASK_PORT）、`CORS_ORIGINS`（环境变量优先/开发预置/生产空）；SECRET_KEY 生产缺失或默认值时 RuntimeError 守卫 |
| `app.py` | 启动块按 DEBUG 分流（开发 Werkzeug / 生产 waitress 16 线程）；HTTP CORS 与 SocketIO CORS 统一用 Config.CORS_ORIGINS；`auto_end_expired_exams` 改每考试独立事务 + 批量预载 score_map/questions 消除 N+1 + IntegrityError 整场回滚；`init_database` 题型/学分修正改 SQL 端过滤 |
| `ai_service/client.py` | 删除模块级 `os.environ` 代理清空（6 行），改 `requests.post(..., proxies={"http": None, "https": None})` 请求级绕过 |
| `requirements.txt` | 新增 `waitress==3.0.2`（生产 WSGI 服务器） |
| `说明文档.md` | 更新至 v3.3（部署加固 + 性能优化全章节同步） |
| `.env`（本地，不进 Git） | 追加 `FLASK_DEBUG=true`（开发模式） |

---

## 2026-09-09（续）— 漏洞排查清单专项修复（说明文档 v3.4）

> 背景：依据《漏洞排查清单.md》17 项对全项目做安全复核，分两轮修复（第一轮 #1-#4、#12，第二轮 #5-#10、#13-#16），每轮均实测验证。**注册自选教师/学生角色按开发测试需要保留**（`api/auth_routes.py` 留有上线前收紧注释）；第 11 项经核查为误判（verify_token 返回 DB User，角色实时读库不读 JWT）。

### 一、严重漏洞修复（7 项）

| # | 问题 | 修复 | 关键文件 |
|---|---|---|---|
| 1 | CORS 来源硬编码 | 统一改用 `Config.CORS_ORIGINS`（该项 v3.3 已完成，本轮复核确认无残留） | `app.py` |
| 2 | **题库/课程/专业写接口无鉴权**：任何匿名用户可增删改题目/专业/课程 | 三模块各加 `_require_teacher()`（未登录 401、非教师 403），覆盖增删改、Excel/CSV 导入、模板下载、附件上传；学生管理接口同步补齐 | `api/question_routes.py`、`api/course_routes.py`、`api/major_routes.py` |
| 3 | **题库 GET 答案泄露**：匿名/学生可拉取题目答案与解析 | 列表/详情接口要求登录；学生角色 `answer`/`analysis` 强制返回空串；列表缓存 key 加 `:t`/`:s` 角色后缀防串缓存 | `api/question_routes.py` |
| 4 | **通知 IDOR**：接口信任 `user_id` 参数，可读取/删除他人通知 | 全部 6 个接口改 `_current_user()` 统一鉴权，身份只认 JWT、废弃 `user_id` 参数；已读/删除校验通知归属，他人资源 403；前端改 `authFetch` 自动带 Bearer | `api/notification_routes.py`、`frontend/src/views/Layout.vue` |
| 5 | **日志上报接口无认证且可伪造** | 两接口均加 `verify_token()`（401）；级别白名单（DEBUG/INFO/WARNING/ERROR，非法降级 INFO）；`_clean()` 剥离 `\r`/`\n` 防日志伪造换行、消息 2000/字段 500 字符截断；`/api/logs/vue` 补入 CSRF 豁免名单使 401 正确返回 | `api/log_routes.py`、`utils/security.py` |
| 6 | **人工评分跨考试篡改**：answer_id 无归属校验，可改任意答卷分数 | 先定位 `ExamSession(exam_id, student_id)`（不存在 404）；Answer 查询加 `session_id` 约束，跨考试/跨学生 answer_id 一律跳过；`manual_score` 非数字 400、负值归零、评语截断 1000 字，返回 `updated` 计数 | `api/result_routes.py` |
| 7 | **save_answer 校验缺失** | 三重校验：会话必须属于路径考试（400）、会话状态必须 `in_progress`（**交卷后禁改答案**）、题目必须经 `ExamQuestion` 校验属于该考试 | `api/execution_routes.py` |

### 二、中等风险修复（4 项）

| # | 问题 | 修复 | 关键文件 |
|---|---|---|---|
| 8 | **学生可见邀请码**：考试详情对任何登录者返回邀请码/链接 | 邀请码/邀请链接仅创建教师返回，学生为 `null`；非创建教师访问详情 403；详情缓存 key 按 `:t`/`:s` 隔离，缓存失效改通配删除（`delete_pattern`）防脏缓存 | `api/exam_routes.py`、`utils/redis_cache.py` |
| 9 | **异常信息泄露**：6 处接口把 `str(e)` 直接返回客户端 | AI 出题、成绩评分详情/成绩分析、交卷批改、题库上传/导入共 6 处改 `logger.exception` 记服务端日志 + 客户端通用提示 | `api/ai_routes.py`、`api/result_routes.py`、`api/execution_routes.py`、`api/question_routes.py` |
| 10 | **print 调试残留** | result_routes、ai_routes 共 4 处 print/调试日志删除或改 `logger.info` | 同上 |
| 13 | **重复交卷**：重复提交会二次触发批改/重复计分 | submit 增加考试-会话匹配 + 状态校验，非 `in_progress` 返回 400「试卷已提交，请勿重复操作」；批改异常改 `logger.exception` + 200 通用提示 | `api/execution_routes.py` |

### 三、轻微项修复（3 项）

| # | 问题 | 修复 | 关键文件 |
|---|---|---|---|
| 12/14 | 上传无体积限制、`/uploads` 无安全头 | 全局 `MAX_CONTENT_LENGTH = 50MB`（超限 413）；上传接口 stream 探测真实字节数双保险；`/uploads/<filename>` 响应加 `X-Content-Type-Options: nosniff` | `app.py`、`api/question_routes.py` |
| 15 | 400 errorhandler 重复注册 | 删除 `utils/security.py` 中被 app.py 覆盖的 `@app.errorhandler(400)`，留注释说明统一由 app.py 处理 | `utils/security.py` |
| 16 | 遗留 `Model.query.get()`（SQLAlchemy 2.0 已弃用） | 5 个文件共 18 处全部迁移为 `db.session.get(Model, id)`，全项目 grep 零残留（`.get_or_404()` 不动） | `api/chapter_routes.py`、`api/execution_routes.py`、`api/notification_routes.py`、`api/result_routes.py`、`ai_service/routes.py` |

### 四、验证记录

- 全部改动文件 `py_compile` 通过，后端重启正常
- 第一轮实测 **34/34 + 9/9 通过**：匿名写题库/专业/课程 401、学生写 403、学生拉题库答案/解析为空、通知越权 403、CSRF 流程正常、上传超限拦截
- 第二轮自动化回归 **13/13 通过**：匿名日志上报 401、带 token 200、含 `\n` 的 message 日志中无换行注入、学生考试详情 `invitation_code` 为 null、非创建教师评分 403、无效/他人会话 save_answer 与 submit 均 400、重复交卷 400
- 端到端：教师上传 PNG → `/uploads/` 响应头含 `nosniff`；临时测试账号（用后即删）与临时文件均已清理

### 修改 / 新增的文件

| 文件路径 | 修改内容 |
|----------|----------|
| `api/log_routes.py` | 整体重写：两接口加登录鉴权、级别白名单、`_clean()` 剥离换行 + 长度截断 |
| `api/result_routes.py` | 评分保存加 ExamSession 定位 + Answer `session_id` 归属约束 + 分数/评语校验；2 处异常泄露收敛；print 调试清除；6 处 `query.get` → `db.session.get` |
| `api/execution_routes.py` | save_answer 三重归属/状态校验、submit 重复提交 400 + 异常收敛；导入补 `ExamQuestion`、logger；2 处 `query.get` 迁移 |
| `api/exam_routes.py` | 考试详情邀请码仅创建教师返回 + 缓存 key 按角色 `:t`/`:s` 隔离 |
| `api/question_routes.py` | 新增 `_require_teacher()` 覆盖全部写接口（含导入/模板/上传）；GET 登录 required + 学生答案/解析脱敏 + 缓存角色后缀；上传 stream 探测字节数；上传/导入异常通用提示；3 处 `query.get` 迁移 |
| `api/course_routes.py` | 新增 `_require_teacher()`，增删改三接口教师专属 |
| `api/major_routes.py` | 新增 `_require_teacher()`，增删改三接口教师专属 |
| `api/notification_routes.py` | 新增 `_current_user()` 统一 JWT 鉴权，废弃 `user_id` 参数，操作他人通知 403；1 处 `query.get` 迁移 |
| `api/chapter_routes.py` | 4 处 `query.get` → `db.session.get` |
| `api/ai_routes.py` | AI 生成失败异常改通用提示；2 处 print 改 logger |
| `ai_service/routes.py` | 2 处 `query.get` → `db.session.get` |
| `app.py` | 全局 `MAX_CONTENT_LENGTH=50MB`；`/uploads` 响应加 `X-Content-Type-Options: nosniff` |
| `utils/security.py` | `/api/logs/vue` 补入 CSRF 豁免；删除被覆盖的重复 400 errorhandler |
| `utils/redis_cache.py` | `invalidate_exam_cache` 改 `delete_pattern` 通配删除，覆盖角色后缀 key |
| `frontend/src/views/Layout.vue` | 通知相关请求改 `authFetch` 自动携带 Bearer token |
| `说明文档.md` | 更新至 **v3.4**：权限模型/数据安全章节加固点、API 认证列全面修正、新增 4.15 日志上报与 4.16 文件上传章节、CSRF 豁免规则、v3.4 更新记录 |

---

## 2026-09-09（再续）— 全项目 IDOR / 鉴权二次审计修复（说明文档 v3.5）

> 背景：v3.4 专项修复后，对全部 **162 个路由端点**（API 蓝图 + Jinja2 传统模板蓝图 + competition/bounty 独立模块）逐一核实"①是否有登录鉴权、②身份是否取自 JWT 而非参数、③资源归属（creator_id/student_id/publisher_id/captain_id/玩家身份）是否校验"，并对疑似漏洞用学生/教师 token **动态实测**确认。审计确认其余模块（result/execution/exam 写接口/student/chapter/competition 全模块/team/pk/play/bounty 审核链/question_bank 传统蓝图等）鉴权与归属校验完整；以下 3 个后端漏洞 + 1 个前端越权调用 + 1 个前端白屏 bug 已修复并回归。

### 一、后端漏洞修复（3 项）

| # | 等级 | 问题 | 修复 | 关键文件 |
|---|---|---|---|---|
| 1 | 🔴 高危 | **考试题目答案泄露**：`GET /api/exams/<id>/questions` 仅要求登录，**不校验学生是否被邀请**，且响应含 `answer` 正确答案——任意学生可直接拉取任意考试（含未受邀、考前/考后）的正确答案；缓存 key 也未按角色隔离 | 该接口为教师组卷管理用途（学生答题走 `/api/exam/<id>/take`，本就不含答案），改为**仅考试创建者（教师）可访问**：非教师 403、非创建者 403；**鉴权前置到缓存读取之前**，避免学生命中含答案的缓存 | `api/exam_routes.py` |
| 2 | 🟠 中危 | **悬赏详情答案泄露**：`GET /api/bounties/<id>` 对所有登录用户返回关联题库题 `target_question` 的 `answer`/`analysis`（绕过 v3.4 题库脱敏，学生可借此拿任意题库题答案）；`Bounty.to_dict()` 返回的自带题目快照 `target_question_snapshot` 内 `answer` 也对所有查看者可见 | ① 关联题库题：`answer`/`analysis` **仅教师可见**（与题库接口脱敏策略一致），学生只见题干 content/options/type/difficulty；② 自带快照：在 `to_dict()` 统一脱敏，快照内 `answer` **仅发布者本人可见**，其余查看者（含悬赏广场列表）只拿到题干/选项 | `bounty/routes.py`、`bounty/models.py` |
| 3 | 🟠 中危 | **成绩答案公布时机缺失（时序泄露）**：Vue 学生端成绩详情 `GET /api/results/<result_id>` 只做归属校验、**无"考试已结束"门槛**，响应直接含 `correct_answer`/`analysis`——学生提前交卷后考试仍在进行（他人还在答题）即可看到全部正确答案外传；Jinja2 端 `view_analysis` 本有结束校验，Vue 端缺失 | 学生仅在**考试结束后**（`status=ended` 教师手动/系统结束，**或**已过 `end_time` 截止时间）才返回 `correct_answer`/`analysis`；考试进行中这两字段返回空串并下发 `answers_released:false`；教师（创建者）不受限；得分、本人作答、对错判断始终可见。前端成绩详情页未公布时显示提示横幅并隐藏"正确答案/解析"两行 | `api/result_routes.py`、`frontend/src/views/ResultDetail.vue` |

### 二、前端修复（2 项）

| # | 问题 | 修复 | 关键文件 |
|---|---|---|---|
| 4 | **学生"成绩查询"页错调教师接口**：菜单"成绩查询"指向的 `ResultList.vue` 原为教师成绩管理页，对每个考试循环调用教师统计接口 `/api/results/exam/<id>`，学生访问必然 403 刷屏（后端拒绝本身正确，是前端调错接口） | 页面**按角色分流**：教师保留原"成绩管理"（考试列表 + 平均分/最高分统计）；学生改调 `/api/results/me` 展示**本人成绩卡片**（考试名、我的得分/总分、提交时间），"查看详情"跳转 `/results/<id>` | `frontend/src/views/ResultList.vue` |
| 5 | **"我的考试"页白屏 + 控制台告警**：`StudentDashboard.vue` 模板使用 `<CheckCircle />`，但 `CheckCircle` 并非 `@element-plus/icons-vue` 的有效导出（正确名称为 **`CircleCheck`**），ESM 命名导入得到 `undefined`，组件解析失败导致整页无法渲染 | 图标改为有效导出 `CircleCheck` 并正确 import；控制台 `Failed to resolve component: CheckCircle` 告警消除 | `frontend/src/views/StudentDashboard.vue` |

### 三、验证记录

- 全部改动后端文件 `py_compile` 通过，Flask 后端重启正常；前端 Vite 热更新
- **漏洞 1 实测**：学生 GET 考试 2/4 题目由 200（含答案 `'C'`/`'错误'`）变为 **403**、无答案；教师（创建者）仍 200 含答案；匿名 401
- **漏洞 2 实测**：学生看悬赏详情，关联题库题 `answer`/`analysis` 不返回（题干可见）、自带快照无 `answer`；教师可见题库题答案/解析；发布者可见自己快照的 `answer`；悬赏广场列表学生视角快照零答案泄露
- **漏洞 3 三场景实测**（临时切换考试状态后已恢复）：考试 `ended` → 学生 `answers_released=true`，可见正确答案 C/B + 完整解析；考试 `published`+未来截止时间 → 学生 `answers_released=false`、答案全空但得分/本人作答可见，教师仍可见全部；恢复 `ended` 后学生重新可见
- **前端实测**：学生"成绩查询"显示 3 张本人成绩卡片（随堂测试 4/5、Python小测验 1/2、测验 0/2），网络面板仅 `/api/results/me` 200、**不再**请求 `/api/results/exam/*`，403 消失；"我的考试"页正常渲染（已完成 3、平均分 1.7），控制台无图标告警
- 临时测试数据（临时题/临时悬赏/临时考试状态）用后即清并复核

### 修改 / 新增的文件

| 文件路径 | 修改内容 |
|----------|----------|
| `api/exam_routes.py` | `GET /exams/<id>/questions` 改为仅考试创建者教师可访问（非教师/非创建者 403），鉴权前置到缓存读取之前 |
| `bounty/routes.py` | 悬赏详情关联题库题的 `answer`/`analysis` 仅教师返回，学生只见题干/选项 |
| `bounty/models.py` | `Bounty.to_dict()` 自带题目快照 `answer` 仅发布者本人可见，其余查看者（含广场列表）剔除 |
| `api/result_routes.py` | 成绩详情新增答案公布时机控制：学生考试结束后才返回 `correct_answer`/`analysis`，进行中为空 + `answers_released` 标志；教师不受限；导入补 `ExamStatusEnum`/`utcnow` |
| `frontend/src/views/ResultDetail.vue` | 未公布时显示"考后公布"提示横幅，隐藏正确答案/解析行，保留得分与本人作答 |
| `frontend/src/views/ResultList.vue` | 按角色分流：教师成绩统计 / 学生本人成绩列表（调 `/api/results/me`） |
| `frontend/src/views/StudentDashboard.vue` | 无效图标 `CheckCircle` 修正为有效导出 `CircleCheck`，修复白屏 |
| `说明文档.md` | 更新至 **v3.5**：权限模型/数据安全补 v3.5 加固点、考试题目/成绩详情/悬赏 API 鉴权与字段说明修正、v3.5 更新记录 |

---

## 2026-09-14 — v4.0 架构里程碑：Vue3 SPA 全栈化 + 公共题库 + 自主学习模块 + 品牌重塑

> 这是项目自 v2.1 接入 Vue3 以来最彻底的架构切换。v2.1~v3.5 期间，Jinja2 模板（auth/question_bank/exam/execution/result/ai_service 共 9 个蓝图）与 RESTful API 双轨并存，Flask-Login Session 认证与 JWT 双轨并存；v4.0 一举清除所有模板蓝图、Flask-Login、`templates/` 目录、`static/` 目录，Flask 配置 `template_folder=None` + `static_folder=None` 并直接托管 `frontend/dist`，实现**单端口 SPA 部署**。同时新增自主学习模块（5 张表 + 17 端点 + 6 视图）、Question 公共题库扩展、品牌统一为「智汇学场」。

### 一、架构切换：Flask-Login / Jinja2 全面移除

#### 1.1 Flask 应用入口改造（`app.py`）

| 改动 | 说明 |
|---|---|
| 禁用模板与静态目录 | `Flask(__name__, static_folder=None, template_folder=None)` 不再加载 Jinja2 与 Flask 默认静态目录 |
| 移除 Flask-Login | 删除 `login_manager` 初始化、`login_user/logout_user/current_user` 全局引用、`user_loader` 回调；`verify_token()` 统一为唯一认证方式 |
| 移除 9 个模板蓝图注册 | 删除 `auth_bp`、`question_bp`、`dept_bp`、`course_bp`、`chapter_bp`、`exam_bp`、`execution_bp`、`result_bp`、`ai_bp` 共 9 个蓝图的 import 与 register_blueprint |
| API 蓝图统一注册 | 剩余 19 个 Blueprint 通过列表 `api_bps = [...]` 循环注册，统一 `url_prefix='/api'`；新增 `api_learning_bp` |
| 新增 SPA 托管层 | `FRONTEND_DIST` 定位 `frontend/dist`；`/assets/<path:filename>` 长缓存（`max-age=31536000, immutable`）托管带哈希的 js/css；`/` 和 `/<path:path>` catch-all 路由，`api/`、`socket.io`、`uploads/` 前缀及带文件后缀的路径不回退，其余回退 `_spa_index_response()`；dist 未构建时返回友好提示静态 HTML |
| 根路由重写 | 原 `if current_user.is_authenticated: redirect(...)` + `render_template('home.html')` 彻底删除，现在所有请求（除保留前缀）走 SPA catch-all |
| 错误响应 | 浏览器页面错误从 `render_template_string` 改为 SPA 静态 HTML 错误页（占位符 `__CODE__`/`__MESSAGE__` 替换） |

#### 1.2 删除的目录与文件（物理删除）

| 路径 | 行数（约） | 说明 |
|---|---|---|
| `templates/`（27 个 HTML 文件） | ~3000 | 全部 Jinja2 模板：auth/login/register、question_bank 全套、exam 全套、exam_execution 全套、result 全套、ai_service/generate、layout/header 等 |
| `static/` | ~10MB | Bootstrap 4、jQuery、MathJax、自定义 CSS/JS 等静态资源 |
| `auth/routes.py` | ~100 | Flask-Login 认证蓝图（`@login_required` + Session） |
| `question_bank/routes.py` + 4 个子文件 | ~800 | 题库/专业/课程/章节模板端蓝图 |
| `exam/routes.py` | ~500 | 考试模板端蓝图 |
| `exam_execution/routes.py` | ~400 | 考试执行模板端蓝图（含切屏强制交卷逻辑已迁移至 `api/execution_routes.py`） |
| `result/routes.py` | ~300 | 成绩模板端蓝图 |
| `ai_service/routes.py` | ~150 | AI 出题页面模板蓝图（业务逻辑保留在 `services.py`） |
| `app.spec` 旧 PyInstaller 缓存 + `build/` 目录 | — | 清理旧产物 |
| `migrations/env.py` 旧 fileConfig 日志冲突修复残留 | — | 之前 0003 迁移已处理 fileConfig 不摧毁 root logger |

#### 1.3 依赖与配置清理

| 位置 | 改动 |
|---|---|
| `requirements.txt` | 删除 `Flask-Login==0.6.3` |
| `app.spec` hiddenimports | 删除 `'flask_login'` |
| `database/models.py` | 保留 `from flask_login import UserMixin` 仅为兼容历史代码（UserMixin 的 is_authenticated 等方法 app.py 不再引用） |

### 二、新增自主学习模块（v4.0 核心）

#### 2.1 数据库层（`database/models.py`，5 张新表）

| 表 | 字段概要 | 关键约束 | 说明 |
|---|---|---|---|
| **WrongAnswerRecord** | user_id, question_id, source_type, source_id, wrong_answer, correct_answer, is_mastered, wrong_count, last_wrong_at, mastered_at | UniqueConstraint(user_id, question_id) | 错题本；唯一约束实现幂等累加（重复收录 wrong_count+1 而非新增行）；source_type: exam/competition/practice |
| **PracticeSession** | user_id, title, major_id, course_id, status, questions_count, correct_count, total_time_sec, start_time, end_time | cascade='all, delete-orphan' 到 PracticeAnswer | 自由刷题会话；不计分不计时，纯练习；status 硬编码：in_progress/completed |
| **PracticeAnswer** | session_id, question_id, student_answer, is_correct, time_spent_sec, answered_at | FK→PracticeSession | 单题作答；start 时预存占位行供刷新恢复；随会话级联删除 |
| **StudyPlan** | user_id, title, description, target_count, completed_count, major_id, course_id, chapter_id, start_date, end_date, status | — | 学习计划；status: active/paused/completed；completed_count 随练习/考试自动更新 |
| **StudyLog** | user_id, log_type, reference_id, total_questions, correct_count, score_ratio, time_spent_sec, studied_at | — | 学习日志；考试/竞赛/练习结束后聚合写入；log_type: exam/competition/practice |

#### 2.2 API 层（`api/learning_routes.py`，蓝图 `api_learning_bp`，统一 `/api` 前缀，**17 个端点**）

| 子模块 | 端点 | 方法 | 说明 |
|---|---|---|---|
| **错题本** | `/learning/wrong-records` | GET | 列表，支持 source_type/mastered 筛选 |
| | `/learning/wrong-records/<id>/master` | POST | 标记已掌握 |
| | `/learning/wrong-records/<id>` | DELETE | 删除错题 |
| **刷题** | `/learning/practice/start` | POST | 开始刷题（按 major/course 抽题 + 预存 PracticeAnswer） |
| | `/learning/practice/<id>` | GET | 会话详情（刷新恢复） |
| | `/learning/practice/<id>/answer` | POST | 单题作答 |
| | `/learning/practice/<id>/submit` | POST | 提交整套（聚合 correct_count/questions_count） |
| | `/learning/practice/<id>` | DELETE | 删除会话 |
| | `/learning/practice/history` | GET | 历史列表 |
| **学习计划** | `/learning/plans` | GET | 列表 |
| | `/learning/plans` | POST | 创建 |
| | `/learning/plans/<id>` | GET | 详情 |
| | `/learning/plans/<id>` | PUT | 更新 |
| | `/learning/plans/<id>/pause` | POST | 暂停/恢复（active ↔ paused） |
| | `/learning/plans/<id>` | DELETE | 删除 |
| **学习报告** | `/learning/report/overview` | GET | 总览（总题数/正确率/累计时间/错题数/活跃计划数） |
| | `/learning/report/daily` | GET | 每日聚合（`days` 参数，默认 7） |
| **AI 答疑** | `/learning/ask` | POST | 传入 question_id + 问题文本，调用 `ai_service.client.ask_question()` |

权限：全部端点仅校验登录（`verify_token`），不区分教师/学生角色。

#### 2.3 业务钩子（自动收录错题）

`api/execution_routes.py` 考试提交 → AI 批改 → **调用 `sync_wrong_from_exam()`** → 将答错题目幂等写入 WrongAnswerRecord（唯一约束保证重复收录只累加 wrong_count，不重复建行）。捕获异常不阻塞主流程（考试提交与 AI 批改正常完成）。

#### 2.4 Vue3 前端视图（6 个新文件，41 总视图）

| 视图 | 说明 |
|---|---|
| `LearningHome.vue` | 自学首页，青色渐变入口卡（`#06b6d4`），Portal/Layout 侧边栏新增独立入口 |
| `WrongNotebook.vue` | 错题本列表，支持按来源筛选、标记已掌握、**AI 答疑弹窗**（调 `/api/learning/ask`） |
| `PracticeList.vue` | 刷题历史列表 + 开始新刷题（选专业/课程） |
| `PracticeDetail.vue` | 刷题详情，PracticeSession 刷新恢复依赖 PracticeAnswer 预存行 |
| `StudyPlan.vue` | 学习计划 CRUD + 暂停/恢复/进度追踪 |
| `StudyReport.vue` | 学习报告（概览卡片 + 每日折线图） |

### 三、Question 公共题库扩展

#### 3.1 数据模型（`database/models.py`）

| 改动 | 说明 |
|---|---|
| `creator_id = db.Column(db.Integer, db.ForeignKey('user.id'))` | 题目创建者；**允许 NULL**（存量历史题目兼容） |
| `is_public = db.Column(db.Boolean, nullable=False, default=False)` | 是否公开到公共题库 |
| `creator = db.relationship('User', foreign_keys=[creator_id])` | 关联 User |

#### 3.2 API 层（`api/question_routes.py`）

| 改动 | 说明 |
|---|---|
| `GET /api/questions` 新增 `scope` 参数 | `all`(默认) 全部可见 / `mine` 仅自己创建 / `public` 公共题库跨教师 |
| `PUT /api/questions/<id>` 新增归属校验 | `if question.creator_id != current_user.id: 403` |
| `DELETE /api/questions/<id>` 新增归属校验 | 同上 |
| `POST /api/questions/<id>/toggle_public` | 新增单端点切换公开状态，仅创建者可操作 |

#### 3.3 前端接入（`QuestionList.vue`）

- Scope 切换下拉（All / Mine / Public）
- 公开徽章（黄色 `🔓 Public` / 灰色 `🔒 Private`）
- 非创建者看到的题目：编辑/删除按钮禁用或隐藏（调后端 403 兜底）

### 四、考试级联删除修复（v4.0 Bug Fix）

**问题**：旧 Exam 模型没有配置 ORM 级联，直接 `DELETE FROM exam WHERE id=X` 会被外键约束阻塞，或者残留孤儿行 `exam_question/exam_student/exam_session/result/answer`。

**修复**（`database/models.py`）：

| 模型 | 新增级联 |
|---|---|
| `Exam.exam_questions` | `cascade='all, delete-orphan'` |
| `Exam.exam_students` | `cascade='all, delete-orphan'` |
| `Exam.sessions` | `cascade='all, delete-orphan'` |
| `Exam.results` | `cascade='all, delete-orphan'` |
| `ExamSession.answers` | `cascade='all, delete-orphan'` |

删除考试后，关联的 ExamQuestion/ExamStudent/ExamSession/Result/Answer 全部自动清理。

### 五、前端功能接入（4 项 v4.0 新增）

#### 5.1 切屏检测（`ExamTake.vue`）

- 监听 `visibilitychange` + `window.blur` 事件
- 每次切屏 `POST /api/exams/<id>/report_switch`
- 3 次切屏后端强制交卷（`switch_count >= 3` → `_finalize_submission` 本地判分）
- 红色警告条显示当前切屏次数 / 允许次数

#### 5.2 竞赛题目级删除（`CompetitionManage.vue`）

- 新增「管理题目」弹窗，列表展示竞赛所有题目
- 每行「删除」按钮 → `DELETE /api/competitions/<id>/questions/<cq_id>`
- 后端校验竞赛归属 + 状态非进行中

#### 5.3 通知单条删除（`Layout.vue`）

- 通知抽屉每条 hover 时显示关闭按钮（×）
- `DELETE /api/notifications/<id>`
- 后端校验归属（非本人通知 403）

#### 5.4 考试移除学生 + 智能组卷 + 邀请码加入

| 功能 | 文件 | 端点 |
|---|---|---|
| 取消邀请学生 | `api/student_routes.py` | `POST /api/exams/<id>/remove_student/<sid>` |
| AI 智能组卷 | `api/exam_routes.py` | `POST /api/exams/<id>/smart_composition`（按 major/type/difficulty/source_filter 抽题） |
| 邀请码加入 | `api/auth_routes.py` | `POST /api/exam/join/<code>`，StudentDashboard.vue URL 参数 `?join=CODE` 自动填充输入框 |

### 六、品牌重塑：「智汇学场」

全系统统一品牌名，所有入口文本、页面标题、导航品牌统一为「智汇学场 / Smart Learning Arena」。

| 位置 | 改动 |
|---|---|
| `index.html` title | `智汇学场 · 一体化智能学习平台` |
| `Home.vue` navbar 品牌 / Hero CTA / footer | 统一品牌名 |
| `Login.vue` / `Register.vue` | 顶部 Logo 区品牌 |
| `Portal.vue` | 标题「教师 / 学生 工作台」下方品牌 |
| `VideoModal.vue` | footer 品牌 + Canvas 5 张幻灯片末尾「智汇学场」 |
| `app.py` SPA 未构建提示 HTML | 标题「前端未构建」保持通用，但整体品牌统一 |
| `app.py` 错误页 title | `__CODE__ - 智汇学场` |
| `说明文档.md` 标题 | 改为「智汇学场 — 一体化智能学习平台 开发文档 v4.0」 |

### 七、VideoModal.vue：Canvas 5 场景增强（阴影 + 径向渐变）

VideoModal.vue 是首页核心视觉组件，v4.0 全面增强绘制效果：

| 改动 | 说明 |
|---|---|
| 新增 7 辅助函数 | `setShadow()` / `clearShadow()` / `drawGlassCard()`（玻璃拟态卡片）/ `drawGradientBar()`（水平渐变条）/ `drawGradientCircle()`（径向渐变圆）/ `drawGlowRing()`（发光描边环）/ `drawGradientText()`（渐变填充文字） |
| 5 张幻灯片全部增强 | Slide 1 在线考试（玻璃卡 + 紫色阴影）；Slide 2 答题竞赛（渐变进度条 + 发光头像）；Slide 3 征集悬赏（主题阴影 + 渐变标签）；Slide 4 自主学习（渐变气泡 + 雷达图）；Slide 5 闭环总结（发光环 + 径向渐变中心） |
| 绘制规范统一 | 阴影不叠加（每次绘制后立即 clearShadow）、顶部高光渐变模拟光照方向、彩色投影与主题色统一、发光效果（大 blur 值）与投影（小 blur 值）区分 |

### 八、删除的后端蓝图 / 保留的业务逻辑去向

| 已删除模板蓝图 | 业务逻辑去向 |
|---|---|
| `auth/routes.py`（Session 登录/注册/登出） | → `api/auth_routes.py`（JWT，功能完整覆盖） |
| `question_bank/routes.py`（模板端 CRUD） | → `api/question_routes.py` + `api/major_routes.py` + `api/course_routes.py` + `api/chapter_routes.py` |
| `exam/routes.py`（模板端考试管理） | → `api/exam_routes.py` + `api/student_routes.py` |
| `exam_execution/routes.py`（模板端答题 + 切屏强制交卷） | → `api/execution_routes.py`（切屏强制交卷逻辑完整迁移） |
| `result/routes.py`（模板端成绩 + 人工评分） | → `api/result_routes.py` |
| `ai_service/routes.py`（AI 出题页面） | → AI 出题/解析/答疑全部通过 `api/ai_routes.py` + `api/learning_routes.py` 暴露；核心逻辑保留在 `ai_service/services.py` + `ai_service/client.py` |
| Flask-Login `login_manager` | → 彻底移除，`verify_token()` 统一认证 |

### 九、修改 / 新增的文件清单（v4.0）

| 文件 | 操作 | 关键改动 |
|---|---|---|
| `app.py` | 修改 | 移除 Flask-Login / 9 模板蓝图；19 API 蓝图列表注册；SPA 托管层（assets 长缓存 + catch-all + _spa_index_response + SPA_MISSING_HTML）；根路由重写；错误处理器改 SPA 静态 HTML |
| `database/models.py` | 修改 | Question 加 `creator_id` + `is_public` + `creator` relationship；**新增 5 张自学表** WrongAnswerRecord/PracticeSession/PracticeAnswer/StudyPlan/StudyLog；Exam/Session 加级联 delete-orphan；保留 `UserMixin` 仅兼容 |
| `requirements.txt` | 修改 | 删除 `Flask-Login==0.6.3` |
| `app.spec` | 修改 | hiddenimports 删除 `'flask_login'`；PyInstaller 打包路径改为 `frontend/dist` |
| `api/learning_routes.py` | **新增** | 自学 API 蓝图（17 端点） |
| `api/question_routes.py` | 修改 | GET 加 `scope` 参数 + 角色/公开筛选；PUT/DELETE 加 creator_id 归属校验；新增 `toggle_public` 单端点 |
| `api/exam_routes.py` | 修改 | 新增 `POST /exams/<id>/smart_composition` 智能组卷；邀请码加入参数处理 |
| `api/student_routes.py` | 修改 | 新增 `POST /exams/<id>/remove_student/<sid>` 取消邀请 |
| `api/execution_routes.py` | 修改 | 切屏强制交卷逻辑从 `exam_execution/routes.py` 迁移过来（JWT 用户获取 + ExamSession.status 校验）；考试提交后调用 `sync_wrong_from_exam()` 错题收录钩子 |
| `api/notification_routes.py` | 修改 | 通知归属校验统一 JWT；单条删除端点 |
| `utils/security.py` | 修改 | 移除 Flask-Login 相关 import；`verify_token()` 作为唯一认证工具 |
| `config.py` | 修改 | Flask 初始化不再配置 Flask-Login |
| `说明文档.md` | **全量重写** | v4.0：项目结构树删除 auth/question_bank/exam/execution/result 5 个模板目录 + ai_service/routes.py；蓝图表 27→19；枚举表补自学状态；数据表加 Question 新字段 + 5 张自学表；根路由改 SPA catch-all；AI 服务改两层；删除传统模板路由 5.1-5.6 章节；RESTful API 文档补 scope/smart_composition/remove_student/toggle_public + 新增 4.17 自学模块 17 端点；错误处理改 SPA；mermaid 依赖图重绘 |
| `更新日志.md` | 修改 | 标题改为「智汇学场」；v4.0 更新记录（本条目） |
| `frontend/src/views/Home.vue` | 修改 | 品牌名统一为「智汇学场」；Canvas 幻灯片内容更新（四模块 + 闭环） |
| `frontend/src/views/Portal.vue` | 修改 | 4 模块入口卡（新增自学青色渐变 #06b6d4） |
| `frontend/src/views/Layout.vue` | 修改 | 侧边栏新增自学入口卡；通知抽屉 hover 显示 × 关闭按钮（调 DELETE /api/notifications/<id>）；Socket.IO 个人房间监听 |
| `frontend/src/views/QuestionList.vue` | 修改 | scope 切换 + 公开徽章 + 创建者编辑权限控制 |
| `frontend/src/views/ExamTake.vue` | 修改 | `visibilitychange` + `blur` 监听 → POST `/report_switch`；红色警告条；3 次强制交卷提示 |
| `frontend/src/views/CompetitionManage.vue` | 修改 | 新增题目管理弹窗 + `DELETE /api/competitions/<id>/questions/<cq_id>` |
| `frontend/src/views/StudentDashboard.vue` | 修改 | 邀请码加入输入框，支持 URL `?join=CODE` 自动填充 |
| `frontend/src/components/VideoModal.vue` | **全量重写** | 5 张 Canvas 幻灯片 + 7 辅助绘制函数，阴影/径向渐变/玻璃拟态全面增强 |
| `frontend/src/views/LearningHome.vue` | **新增** | 自学首页，青色渐变卡 |
| `frontend/src/views/WrongNotebook.vue` | **新增** | 错题本 + AI 答疑弹窗 |
| `frontend/src/views/PracticeList.vue` | **新增** | 刷题历史 + 开始新刷题 |
| `frontend/src/views/PracticeDetail.vue` | **新增** | 刷题详情（刷新恢复依赖预存行） |
| `frontend/src/views/StudyPlan.vue` | **新增** | 学习计划 CRUD + 暂停/恢复 |
| `frontend/src/views/StudyReport.vue` | **新增** | 学习报告（概览 + 每日） |
| `frontend/src/router/index.js` | 修改 | 新增 6 条自学路由 + Layout 子路由 |
| `frontend/src/stores/auth.js` | 不变 | 已有 |
| `frontend/vite.config.js` | 不变 | 代理 `/api` → 5000 已有 |

### 十、端到端验证（2026-09-14，手动 + 自动）

- **后端 `py_compile`**：`app.py`、所有 `api/*.py`、`competition/*.py`、`bounty/*.py` 全部通过
- **数据库迁移**：Alembic 0001→0002→0003 正常；新增 5 张自学表通过 `db.create_all()` 自动创建（无需新迁移）；Question 新字段 `creator_id`/`is_public` 存量数据默认 NULL/False 不报错
- **后端启动**：`D:\Python312\python.exe app.py` 正常启动（`use_reloader=False`）；`[SPA] Vue3 构建产物托管于: ...frontend\dist` 日志输出；19 Blueprint 全部注册成功
- **SPA 托管**：`http://127.0.0.1:5000/` 直接返回 `frontend/dist/index.html`；`/assets/*.js` 长缓存头 `Cache-Control: public, max-age=31536000, immutable`；`/api/login` 正常返回 JSON，不走 catch-all；`/uploads/*` 不走 catch-all
- **JWT 单轨认证**：登录 → 拿 token → 所有 API 正常；Flask-Login 的 `current_user`/`@login_required`/`login_required` 装饰器引用全项目清零
- **公共题库**：教师创建题目 → `POST /toggle_public` → 其他教师 `GET /api/questions?scope=public` 可见；PUT/DELETE 非创建者返回 403；学生 scope=public 可见但答案/解析脱敏
- **智能组卷**：`POST /api/exams/<id>/smart_composition` 按 major + 难度抽题，自动 invalidate_exam_cache
- **考试级联删除**：删除考试 → 关联的 exam_question/exam_student/exam_session/result 全部清理（SQLite 无孤儿行残留）
- **自学模块**：`GET /api/learning/report/overview` 返回空概览（新用户）；`POST /api/learning/practice/start` 创建会话 + 预存 PracticeAnswer；刷新 PracticeDetail 页面可恢复
- **错题收录钩子**：考试交卷后 WrongAnswerRecord 自动插入（幂等：同一题重复答错 wrong_count+1）
- **前端构建**：`cd frontend && npm run build` 通过（built in 13.24s），无编译错误
- **品牌名**：浏览器标签、登录页、首页、Portal 全部显示「智汇学场」

### 十一、已知注意事项

| # | 事项 | 说明 |
|---|---|---|
| 1 | `app.py` 以 `use_reloader=False` 启动，改后端代码必须手动重启进程（杀 5000 端口进程再启动） | — |
| 2 | 前端开发推荐 `npm run dev`（5173 端口 Vite 代理）；生产部署需先 `npm run build` 生成 dist 再启动 Flask | — |
| 3 | Redis 未运行属正常，自动降级数据库查询 | — |
| 4 | 数据库 SQLite：日期统一 `%Y-%m-%d %H:%M:%S.%f` 空格分隔（`strftime`），裸 SQL 禁止写 isoformat `T` 分隔符 | — |
| 5 | Question 旧数据 `creator_id` 为 NULL，这些题目所有人都可以编辑/删除（历史兼容） | — |
| 6 | 自学模块状态值硬编码字符串（`in_progress`/`completed`/`active`/`paused`），无 Enum 类 | — |
| 7 | 已删除的 Jinja2 模板若需恢复，只能从 git 历史回滚 | — |

---

## 2026-09-15 — v4.0.1 热修复：交卷链路 P0 崩溃 + 隐患审计 #4–#12 评估处置

> v4.0 发布次日代码审计发现：SPA 迁移时交卷链路存在一个**必现 TypeError**及三个连锁缺陷，学生交卷后永远没有成绩；随后对两轮隐患报告（#4–#12）逐条核实，采纳 7 项、驳回 2 项。所有修复均经 py_compile + 后端重启冒烟验证。

### 一、P0：交卷链路必崩 + 三个连锁缺陷（审计发现，非用户上报）

#### 1.1 `ai_grade_exam` 函数签名不匹配——交卷必抛 TypeError

| 项 | 内容 |
|---|---|
| 现象 | [services.py] 定义 `ai_grade_exam(db, exam_id, student_id)`（3 参），[execution_routes.py] 调用 `ai_grade_exam(db.session, session.id)`（仅 2 参）→ 运行时 `TypeError: missing 1 required positional argument: 'student_id'`；异常被 submit 的 except 吞掉返回 200「批改处理中」，表面无感知 |
| 第二层错配 | 即使补齐参数数量也仍是错的：调用处第二参传的是 **session.id**，形参却是 **exam_id**，函数内部 `ExamSession.query.filter_by(exam_id=<会话ID>)` 必查不到 → ValueError |
| 修复 | 签名统一为 **`ai_grade_exam(session, session_id)`**（最终形态，#9 进一步把首参从 db 正名为 session）：内部 `session.get(ExamSession, session_id)` 自行取 exam_id；两个调用场景（手动交卷/切屏强制交卷）手里都只有会话 ID，从根上杜绝双标识错配 |

#### 1.2 手动交卷永不创建 Result 成绩记录

- `ai_grade_exam` 只写 Answer、返回 `(earned, total)`，但 submit 既不接收返回值也不建 `Result(...)`；而会话已置 `submitted`，scheduler 自动收卷只捞 `status='in_progress'` 的会话，**无法兜底** → 学生 `/report` 404、`/results/me` 永远无成绩
- 修复：抽取统一结算函数 **`_finalize_submission(sess)`**（[execution_routes.py]），流程：查重（exam_id+student_id 幂等）→ AI 批改 → 建 Result → `flush()` 抢占唯一约束（并发交卷竞态时回滚并返回既有记录）→ 成绩通知（`commit=False`）→ 单次 commit → 错题收录

#### 1.3 错题自动收录从未生效

- `sync_wrong_from_exam` 排在 `ai_grade_exam` 之后同一 try 内，TypeError 直接跳过它；且它依赖 `Answer.is_correct is False`，批改没跑就没有对错标记
- 修复：随 1.1/1.2 一并解决；收录放在成绩 commit 之后，异常仅记日志不阻塞主流程（非关键路径）

#### 1.4 切屏 3 次强制交卷逻辑在 v4.0 迁移时丢失

- v3.x 旧模板端 `exam_execution/routes.py` 有强制交卷，v4.0 迁移到 `api/execution_routes.py` 时 `api_report_switch` 只剩计数 +1，全项目 grep `_finalize_submission` 零命中——前端红色警告条没有服务端强制力
- 修复：`report_switch` 计数后判断 `switch_count >= 3` → 置 `submitted` + commit + 立即 `_finalize_submission`（本地判分），响应新增 `force_submitted` 标志
- 前端 [ExamTake.vue]：两个切屏 handler（visibilitychange / window.blur）合并为 `handleSwitchReport`，收到 `force_submitted` 弹「已强制交卷」警示框并跳首页；`forceLocked` 锁防双事件重复弹窗

### 二、隐患报告 #4–#8 评估处置

| # | 建议 | 核查结论 | 处置 |
|---|---|---|---|
| 4 | SQLite 单写者 → WAL / PostgreSQL | 属实：仅 `connect_args timeout=30`，无 WAL；调度线程/请求线程/SocketIO 线程三方并发写 | ✅ 采纳 WAL；PG 保留上线前 TODO |
| 5 | scheduler 循环内通知逐行 commit | 属实，且隐患更大：第一个学生通知的 commit 会**提前提交 `exam.status=ended`**，若中途崩溃，剩余 in_progress 会话因考试已 ended 永不被调度（只捞 PUBLISHED），成为无成绩孤儿 | ✅ 采纳 |
| 6 | Redis 降级时加进程内 TTLCache | **前半失实**：`invalidate_exam/question/result_cache` 主动失效均存在且写操作都在调用，TTL 仅兜底；Redis 不可用直穿 DB 是既定可接受降级。当前校园单进程规模加 LRU 属过度设计，多 worker 部署时进程内缓存还收不到跨进程失效 → 脏数据 | ❌ 不采纳 |
| 7 | 邀请码 while 查重无上限 | 风险属实（36⁸≈2.82 万亿组合，纯理论级）；报告行号过期（实际 models.py 邀请码方法非 L126） | ✅ 采纳 |
| 8 | 日志上报无限流 | **前提部分失实**：Flask-Limiter 全局默认 60/min 已兜底，并非裸奔；但落盘类接口收紧成本极低 | ✅ 采纳收紧 30/min |

**实施细节：**

1. **WAL 模式（#4）**：[app.py] `db.init_app` 后注册 SQLAlchemy `connect` 事件，每个新连接执行 `PRAGMA journal_mode=WAL` + `PRAGMA synchronous=NORMAL`（WAL 官方推荐搭配）。WAL 下读不阻塞写，写者之间仍串行（由 busy_timeout=30 排队）。实测真实库位于 **`instance/exam_system.db`**（Flask-SQLAlchemy 3.x 相对路径落在 instance/，非项目根目录），切换后 `-wal`/`-shm` 侧车文件出现，数据完好。
2. **通知延迟提交（#5）**：`create_notification(..., commit=True)` 加开关，默认行为不变；`create_exam_result_notification` 透传。scheduler 批量收卷循环内传 `commit=False`，整场考试所有 Result + 通知 + 状态变更在循环外**单次原子 commit**（冲突整场回滚、下轮重试、幂等）；`_finalize_submission` 同样成绩+通知一次提交。
3. **邀请码重试上限（#7）**：`Exam.generate_invitation_code()` 改 `for _ in range(10)` 有限重试，超限抛 `RuntimeError("邀请码生成连续 10 次碰撞，请重试")`。
4. **日志限流（#8）**：`RateLimitConfig` 新增 `LOG_REPORT = "30 per minute"`，`/api/logs` 与 `/api/logs/vue` 两个端点显式 `@limiter.limit`，响应头实测 `X-RateLimit-Limit: 30`。

### 三、隐患报告 #9–#12 评估处置

| # | 建议 | 核查结论 | 处置 |
|---|---|---|---|
| 9 | `db` 参数名歧义，统一 session | **比报告更乱**：`ai_generate_questions`/`ai_grade_exam` 形参叫 `db` 收的却是 `db.session`（内部全是 session API）；`ai_generate_analysis(db, ...)` 的形参**根本没被使用**（函数内另起 `_db.session`），而 learning_routes 调用时传的还是 `db` 对象——靠"忽略参数"碰巧能跑 | ✅ 采纳 |
| 10 | 枚举普通类迁移 StrEnum | 普通类属实，但 DB 列是 String、全项目与裸字符串比较，枚举不承载任何入库约束，迁移零类型安全收益；波及竞赛/悬赏/PK 多个枚举类与 classmethod，纯风格重构 | ❌ 不采纳，记为技术债（若做应连 DB 列一起改 SA Enum + 数据校验，单独立项） |
| 11 | 缺 @log_errors → 返回 HTML 500 | **核心论断不成立**：app.py 已注册 400/401/403/404/405/500 全套 errorhandler，API 请求 `_wants_json()` 一律 JSON，500 还带 `db.session.rollback()`，兜底比 @log_errors 更全。仅 `/register` 缺装饰器属实（影响只是错误日志少函数名前缀） | ⚠️ 仅补 register |
| 12 | `logging.warning` 未导入 | **真 Bug**（行号过期，实际 result_routes.py L178）：人工评分详情接口遇到题目被删分支即 `NameError: logging` → 500 | ✅ 采纳 |

**实施细节：**

1. **session 形参统一（#9）**：services.py 三个函数 `ai_generate_questions` / `ai_grade_exam` / `ai_generate_analysis` 形参全部 `db` → **`session`**，内部统一 `session.get/add/commit`；`ai_grade_exam` 内 ExamSession 局部变量改名 `exam_session` 防与形参冲突；`ai_generate_analysis` 删除无用的 `from database import db as _db`，**真正使用传入 session**；删除文件顶部失效的 `from database import db`。调用方三处对齐：ai_routes 本就传 `db.session`、execution_routes `_finalize_submission` 传 `db.session`、**learning_routes.py:581 由误传 `db` 改正为 `db.session`**。
2. **register 装饰器（#11）**：补 `@log_errors` + `@log_performance`，与 login 完全对齐。
3. **logging NameError（#12）**：`logging.warning(...)` → `logger.warning(...)`（logger 早已导入）；全项目 grep 裸 `logging.(warning|error|info|exception|debug)` 调用确认零残留。

### 四、修改文件清单（v4.0.1）

| 文件 | 改动 |
|---|---|
| `ai_service/services.py` | `ai_grade_exam` 签名改 `(session, session_id)` 并内部自取 exam_id；三函数形参统一 session；ai_generate_analysis 启用传入参数；删除失效 db import |
| `api/execution_routes.py` | 新增 `_finalize_submission()`（幂等 Result + flush 抢占 + 通知 + 错题收录）；submit 复用；report_switch 补切屏 ≥3 强制交卷 + `force_submitted` 响应 |
| `api/notification_routes.py` | `create_notification` / `create_exam_result_notification` 加 `commit` 开关（默认 True 向后兼容） |
| `app.py` | SQLite connect 事件开启 WAL + synchronous=NORMAL；scheduler 收卷通知改 `commit=False` 循环外统一提交 |
| `database/models.py` | `generate_invitation_code` 改 10 次有限重试，超限 RuntimeError |
| `api/log_routes.py` | 两个日志上报端点加 `@limiter.limit("30 per minute")` |
| `utils/security.py` | `RateLimitConfig.LOG_REPORT = "30 per minute"` |
| `api/result_routes.py` | L178 `logging.warning` → `logger.warning`（修复 NameError） |
| `api/auth_routes.py` | register 补 `@log_errors` + `@log_performance` |
| `api/learning_routes.py` | AI 答疑调用改传 `db.session` |
| `frontend/src/views/ExamTake.vue` | 切屏 handler 合并 + force_submitted 弹窗跳首页 + forceLocked 防重入；前端重新 build |

### 五、验证记录（2026-09-15）

- 全部修改文件 `py_compile` 通过；后端重启无导入错误，19 蓝图注册正常，scheduler/SocketIO/SPA 托管日志正常
- 参数链路实测：`ai_grade_exam(db.session, 不存在ID)` 与 `ai_generate_analysis(db.session, 不存在ID, ...)` 均正确抛业务 `ValueError`（非 TypeError）
- 学生登录 200；重复用户名注册返回业务 400 JSON（未新建测试账号，仍只有 nfpz/123 两个账号）
- WAL：`PRAGMA journal_mode` 返回 `wal`，`instance/exam_system.db-wal/-shm` 存在
- 限流：`/api/logs/vue` 响应头 `X-RateLimit-Limit: 30`
- 前端 `npm run build` 通过；全项目裸 `logging.*` 调用零残留

### 六、新增运维注意

1. **WAL 备份方式变更**：直接复制 `.db` 可能漏掉 `-wal` 中未 checkpoint 的最新事务。备份请用 `sqlite3 instance/exam_system.db ".backup backup.db"`，或先执行 `PRAGMA wal_checkpoint(TRUNCATE)` 再复制。
2. 诊断脚本注意：Flask-SQLAlchemy 3.x 相对路径 SQLite 真实文件在 `instance/` 下；在项目根目录对 `exam_system.db` 开连接会**新建 0 字节空库**（本次排查中误建过一个，已删除）。

---

## 项目当前状态（v4.0.1）

| 模块 | 状态 | 说明 |
|------|------|------|
| 系统品牌 | ✅ 统一 | 「智汇学场 / Smart Learning Arena」全系统统一（Home/Login/Register/Portal/VideoModal/错误页/文档） |
| SPA 架构 (v4.0) | ✅ 就绪 | Flask `template_folder=None` + `static_folder=None`，托管 `frontend/dist`；`/assets/*` 长缓存 + catch-all 回退 index.html；单端口 5000 部署 |
| 用户注册/登录 | ✅ 正常 | 邮箱可选，密码 Bcrypt 哈希，JWT Token 认证（7 天有效期） |
| 角色权限控制 | ✅ 正常 | 老师/学生分离，**JWT 单轨**（`verify_token()`，Flask-Login @login_required 已移除） |
| 公共题库 (v4.0) | ✅ 正常 | Question 加 creator_id + is_public；scope=all/mine/public 跨教师共享；仅创建者可 PUT/DELETE/toggle_public；学生答案/解析脱敏 |
| 题库管理（CRUD） | ✅ 正常 | 支持中文显示、按专业/题型/来源筛选、媒体上传；scope 切换；公开徽章 |
| 专业管理 | ✅ 正常 | 删除前级联检查（Course.chapters cascade → Chapter.questions cascade） |
| AI 出题 | ✅ 正常 | 8 种题型，支持历年真题选项，知识点自动匹配 |
| 智能组卷 (v4.0 完善) | ✅ 正常 | `POST /api/exams/<id>/smart_composition` 按 major/type/difficulty/source_filter 抽题；自动 invalidate_exam_cache |
| 考试管理 | ✅ 正常 | 创建/编辑/筛选添加题目/邀请管理/随机打乱/发布/结束/重复发布全流程；级联删除（Exam→题目/学生/会话/成绩、Session→Answer） |
| 移除受邀学生 (v4.0) | ✅ 正常 | `POST /api/exams/<id>/remove_student/<sid>` |
| 邀请设置 | ✅ 正常 | 邀请码 `.upper().strip()` 幂等加入；邀请码/链接/QR码 三种方式；仅创建教师可见，学生为 null；生成查重 10 次上限（v4.0.1） |
| 学生参加考试 | ✅ 正常 | 答题、保存草稿、提交、倒计时同步、切屏检测（visibilitychange/blur）；**v4.0.1 起 3 次切屏服务端真正强制交卷并立即结算**（v4.0 迁移时该逻辑曾丢失，前端收到 force_submitted 弹窗跳首页） |
| 学生仪表板 | ✅ 正常 | 三板块分离（待参加/已结束/成绩）；邀请码加入输入框 + `?join=CODE` URL 自动填充；暗色模式 |
| 邀请码加入 (v4.0) | ✅ 正常 | `POST /api/exam/join/<code>`；StudentDashboard URL 参数 `?join=CODE` 自动填充 |
| 交卷结算 (v4.0.1) | ✅ 正常 | 统一 `_finalize_submission()`：手动交卷/切屏强制交卷共用；幂等 Result（唯一约束 flush 抢占防并发）+ 成绩通知单次原子 commit + 错题收录；修复 v4.0「交卷后永不生成成绩」P0 |
| AI 批改 | ✅ 正常 (v4.0.1) | `ai_grade_exam(session, session_id)` 签名统一；客观题本地比对+主观题AI语义，评分 clamp [0,100] 防异常溢出，失败降级 0 分待人工 |
| AI 答案解析 | ✅ 正常 | 考试结束后才可查看（answers_released 标志，考试 ended 或过截止时间） |
| 教师人工评分 | ✅ 正常 | 主观题逐题评分、写评语、自动重算总分；answer 归属会话校验防跨考试篡改 |
| 成绩可视化分析 | ✅ 正常 | Vue3 图表页，柱状图/环形图/表格多维度展示 |
| 成绩答案公布时机 (v3.5) | ✅ 正常 | 学生仅考试 ended 或过截止时间后可见 correct_answer/analysis，进行中为空但得分/本人作答可见；教师不受限 |
| Vue3 前端 | ✅ v4.0 全面 SPA 化 | 41 视图（新增 6 自学）；Element Plus + Pinia + Vue Router；暗色主题；Portal 4 模块卡；Layout 侧边栏 4 入口；VideoModal Canvas 5 幻灯片 + 7 辅助函数（阴影/径向渐变/玻璃拟态） |
| 模板引擎 | ❌ 已移除 (v4.0) | Jinja2 全部删除，27 个 HTML 文件 + 9 个模板蓝图（auth/question_bank/exam/execution/result/ai_bp 等）已清除 |
| Flask-Login | ❌ 已移除 (v4.0) | requirements.txt 删除 Flask-Login==0.6.3；login_manager/current_user/@login_required 全项目清零；UserMixin 仅保留在 models.py 兼容 |
| 图标库稳定 | ✅ 正常 | Vue3 统一使用 `@element-plus/icons-vue` |
| 说明文档 | ✅ v4.0 | 项目结构树/蓝图表/数据表/API 文档全面更新；含 mermaid 依赖图 + 权限矩阵 |
| 竞赛管理（教师） | ✅ 正常 | 创建/发布/题目管理/报名管理 |
| 竞赛题目级删除 (v4.0) | ✅ 正常 | CompetitionManage 弹窗 + `DELETE /api/competitions/<id>/questions/<cq_id>`；后端校验归属 + 非进行中 |
| 限时积分赛（学生） | ✅ 正常 | 加入/开始/答题/交卷，防作弊抽题固化（Competition.draw_count + assigned_cq_ids） |
| 1v1 PK 对战 | ✅ 正常 | 发起/接受/答题/惰性结算，同分先完成者胜；CAS 原子结算防并发双发积分；WAITING 30 分钟过期自动作废 |
| Socket.IO 实时推送 | ✅ 正常 | 3 类房间推送（竞赛/PK/个人），30s 轮询兜底；Socket.IO 房间鉴权（JWT + PK 仅限对战双方） |
| 段位系统 | ✅ 正常 | 6 档段位（青铜 0 → 王者 1000），积分实时推导，无漂移 |
| 赛季系统 | ✅ 正常 | 自然月赛季，惰性归档（CAS 抢锁），Top3 勋章 |
| 勋章系统 | ✅ 正常 | 8 种勋章（竞赛 7 + 悬赏「悬赏新星🪙」1），事件驱动，幂等发放，实时推送 |
| 征集悬赏 | ✅ 正常 | 题目征集/答案征集双类型，发布-投稿-采纳闭环（师生同权），CAS 防并发采纳，题目采纳自动入题库、答案采纳回填解析；悬赏详情非发布者/非教师隐藏 target_question 的 answer/analysis |
| 悬赏奖励 | ✅ 正常 | 采纳发放竞技积分+勋章，计入赛季段位，Socket.IO 推送，奖励失败不回滚采纳 |
| 战队系统 | ✅ 正常 | 创建/加入/退出/转让/解散，实时聚合积分（=队员当前赛季积分之和，无落库） |
| 学情画像 | ✅ 正常 | 五维雷达图（准确率/速度/竞技力/稳定度/活跃度），考试+竞赛+自学数据，权限隔离 |
| **自主学习模块 (v4.0)** | ✅ 正常 | 5 张表 + 17 API + 6 Vue 视图；错题本（自动收录 + 幂等 wrong_count 累加）；自由刷题（PracticeAnswer 预存供刷新恢复）；学习计划（暂停/恢复）；每日学习报告；AI 答疑 |
| 错题收录钩子 (v4.0.1 真正生效) | ✅ 正常 | 考试提交后 `sync_wrong_from_exam()` 自动写入 WrongAnswerRecord（成绩 commit 之后执行，捕获异常不阻塞主流程）；v4.0 时因前置 ai_grade_exam TypeError 从未实际跑到，v4.0.1 修复 |
| requirements.txt | ✅ 精确版本 | 已移除 Flask-Login；精确版本号 Python 依赖清单 |
| 时区统一 | ✅ 正常 | aware datetime 全链路，DB 存 UTC，展示 Asia/Shanghai，历史数据已归一化；UTCDateTime 自定义 TypeDecorator |
| 数据库迁移 | ✅ 正常 | Flask-Migrate/Alembic，0001 基线→0002 时区→0003 结构+约束；5 张自学表通过 `db.create_all()` 自动创建（无需新迁移）；Question 新字段 creator_id/is_public 存量兼容 NULL/False |
| SQLite 并发 (v4.0.1) | ✅ 已加固 | WAL 日志模式 + synchronous=NORMAL（connect 事件自动 PRAGMA）；读不阻塞写，busy_timeout=30 排队写者；scheduler 批量收卷改整场单事务原子提交；真实库在 `instance/exam_system.db`；备份需 `.backup` 或先 wal_checkpoint |
| 后台定时任务 | ✅ 正常 | `utils/scheduler.py` daemon 线程（60s），过期收卷/竞赛结算/赛季归档脱离请求 |
| 写入幂等 | ✅ 正常 | Result/UserPointsProfile/UserBadge/WrongAnswerRecord/PracticeAnswer 唯一约束 + IntegrityError 兜底防并发 |
| CSRF 防护 | ✅ 正常 | Bearer 请求自动豁免，公开接口白名单，其余强制校验 |
| 日志系统 | ✅ 正常 | SafeRotatingFileHandler（10MB 轮转，Windows 占用安全降级），高频路径静默（/api/logs*、/socket.io、/api/csrf-token、静态资源），werkzeug 提 WARNING，Alembic fileConfig 守卫 |
| verify_token 统一 | ✅ 正常 | 全部 API 模块（含 competition/bounty/learning）统一引用 utils/security.py，无重复实现 |
| 防作弊加固 | ✅ 正常 | 抽题范围校验、time_spent 服务端计算（`answered_at` 差值，客户端传值忽略）、切屏 3 次强制交卷、Socket.IO 房间鉴权 |
| PK 对战安全 | ✅ 正常 | CAS 原子结算防并发双发积分、WAITING 30 分钟过期自动作废 |
| options 存储统一 | ✅ 正常 | AI/表单/API 三路径统一存 JSON 数组（`question_routes._normalize_options`），前端 JSON.parse 一致 |
| AI 评分安全 | ✅ 正常 | 百分比 clamp [0,100] 再换算满分，防异常溢出 |
| 错误处理 | ✅ 正常 | 全局 400/401/403/404/405/500 errorhandler：API 返回 JSON（500 带 session rollback），浏览器返回 SPA 静态 HTML 错误页；`_wants_json()` 区分 API/页面；inject_notifications 跳过 /api 路径；register 已补齐 @log_errors（v4.0.1） |
| 考试执行安全 | ✅ 正常 | 会话存在性/归属/跨考试校验，submit POST-only（GET 405） |
| 题目删除守卫 | ✅ 正常 | 被考试引用的题目禁止删除（Question 级联到 ExamQuestion + API 双守卫）；公共题库新增 creator_id 归属校验（非创建者 403） |
| lobby 性能 | ✅ 正常 | 批量聚合 question/participant 计数，消除 N+1 查询；get_my_rank 复用行 |
| 生产部署配置 | ✅ 就绪 (v3.3) | DEBUG/SECRET_KEY/CORS_ORIGINS/HOST/PORT 全部环境变量化；生产自动启用 waitress；SECRET_KEY 缺失或默认值启动即拒绝（fail-fast）；CORS 生产默认不开放跨域源 |
| 过期收卷事务 | ✅ 正常 (v3.3) | 每考试独立事务（单考失败不影响整批）+ 批量预载消除 N+1，缩短 SQLite 写锁持有 |
| AI 请求代理 | ✅ 正常 (v3.3) | 请求级 `proxies=` 绕过系统代理，无全局 os.environ 副作用 |
| 启动数据修正 | ✅ 正常 (v3.3) | 题型小写/课程学分改 SQL 端过滤，稳态 0 行扫描，保留自愈 |
| 题库/课程/专业鉴权 | ✅ 正常 (v3.4) | 写接口统一 `_require_teacher()`（401/403）；GET 题库登录 required，学生答案/解析脱敏，缓存按角色隔离；**v4.0 新增** creator_id 归属校验（PUT/DELETE/toggle_public） |
| 通知接口安全 | ✅ 正常 (v3.4) | 身份只认 JWT，废弃 `user_id` 参数，IDOR 防护（他人通知 403）；**v4.0 新增** 单条删除（DELETE /api/notifications/<id>） |
| 日志上报安全 | ✅ 正常 (v3.4/v4.0.1) | 登录鉴权 + 级别白名单 + 剥离换行防伪造 + 长度截断；**v4.0.1 限流收紧为 30/min**（`RateLimitConfig.LOG_REPORT`，此前仅全局默认 60/min 兜底） |
| 评分/答题/交卷归属 | ✅ 正常 (v3.4) | 评分 answer_id 会话归属约束防跨考试篡改；save_answer 考试-会话-题目三重校验；交卷后锁定，重复提交 400 |
| 邀请码可见性 | ✅ 正常 (v3.4) | 仅创建教师可见，学生 null；缓存 `:t`/`:s` 角色隔离 + 通配失效 |
| 上传安全 | ✅ 正常 (v3.4) | 全局 50MB 上限（413）+ stream 探测双保险；`/uploads` 响应 nosniff；UUID 命名 + 图片/视频白名单 |
| 异常信息收敛 | ✅ 正常 (v3.4) | 6 处接口异常详情只写服务端日志，客户端通用提示；print 调试残留清零 |
| SQLAlchemy 2.0 | ✅ 正常 (v3.4) | 全项目 `query.get` 零残留，统一 `db.session.get` |
| 考试题目接口鉴权 | ✅ 正常 (v3.5) | `GET /api/exams/<id>/questions` 仅考试创建者教师可访问（非教师/非创建者 403），鉴权前置缓存读取；学生答题走不含答案的 take 接口 |
| 悬赏答案脱敏 | ✅ 正常 (v3.5) | 悬赏详情关联题库题 answer/analysis 仅教师可见；自带题目快照 answer 仅发布者本人可见（广场列表同步覆盖） |
| 学生成绩查询页 | ✅ 正常 (v3.5) | ResultList 按角色分流：学生调 `/api/results/me` 展示本人成绩卡片，不再循环调用教师接口刷 403 |
| GitHub 仓库 | ✅ 已同步 | https://github.com/Gmsfzy/exam-system-Beta main 分支为最新代码（含 v4.0 SPA 化 + 自学模块 + 公共题库全部改动） |

---

## 待办事项

- [x] ~~完善学生端计时防作弊机制（倒计时同步、切屏检测）~~（已完成）
- [x] ~~修复 `LegacyAPIWarning`：`Query.get()` → `Session.get()`（SQLAlchemy 2.0 兼容）~~（已完成，全部 10 处替换）
- [x] ~~支持考试截止时间自动触发结束（定时任务）~~（已完成，已从 `before_request` 迁移到 `utils/scheduler.py` 后台线程）
- [x] ~~考试管理添加题目页面增强（筛选/删除/打乱/防重复）~~（已完成）
- [x] ~~学生邀请管理增强（取消邀请/全局返回）~~（已完成）
- [x] ~~考试提交失败不跳转修复（AI 批改错误处理/loading 遮罩）~~（已完成）
- [x] ~~考试解析权限控制（双重判断/提交后只看成绩）~~（已完成）
- [x] ~~学生仪表板三板块分离（待参加/已结束/成绩）~~（已完成）
- [x] ~~题目批量导入/导出功能（CSV 模板下载 + 批量导入 + 筛选导出）~~（已完成）
- [x] ~~考试成绩统计图表（柱状图/饼图可视化）~~（已完成，Vue3 ResultAnalysis 页面）
- [x] ~~配置真实大模型 API 密钥~~（已配置豆包 API）
- [x] ~~邀请设置（邀请码/邀请链接/QR码生成功能）~~（已完成，Jinja2 + Vue3 双端支持）
- [x] ~~考试删除入口统一，避免功能重复冲突~~（已完成，仅保留菜单中"删除考试"选项）
- [x] ~~已结束考试修改时间后可重复发布~~（已完成）
- [x] ~~完善考试管理流程~~（已完成）
- [x] ~~添加 AI 批改结果展示页面~~（已完成）
- [x] ~~添加 AI 答案解析展示页面~~（已完成）
- [x] ~~Jinja2 模板添加题目输入框富文本编辑功能（格式化按钮 + 数学符号插入）~~（已完成）
- [x] ~~Jinja2 模板欢迎首页创建（导航栏、Hero、功能展示、主题切换）~~（已完成）
- [x] ~~Jinja2 模板首页亮暗主题切换功能~~（已完成）
- [x] ~~Vue3 模板首页创建（Element Plus 图标、主题切换、暗色模式）~~（已完成）
- [x] ~~Vue3 图标库回退到 Element Plus，移除问题较多的 lucide-vue-next~~（已完成）
- [x] ~~视频演示功能实现（Canvas动画录制、视频控件、图标统一）~~（已完成）
- [x] ~~竞赛竞技化三期：Socket.IO 实时榜单~~（已完成，3 类房间推送 + 30s 轮询兜底）
- [x] ~~段位系统（6 档，积分实时推导）~~（已完成）
- [x] ~~赛季系统（自然月 + 惰性归档 + CAS 抢锁）~~（已完成）
- [x] ~~勋章系统（7 种，事件驱动，实时推送）~~（已完成）
- [x] ~~战队系统（CRUD + 实时聚合积分）~~（已完成）
- [x] ~~学情画像（五维雷达图）~~（已完成）
- [x] ~~requirements.txt 精确版本号依赖清单~~（已完成）
- [x] ~~时区统一：引入 Flask-Migrate/Alembic，历史数据一次性补时区，切到 aware datetime~~（已完成，0002 迁移 + UTCDateTime）
- [x] ~~Result 加唯一约束防并发重复记录~~（已完成，UniqueConstraint + IntegrityError 兜底）
- [x] ~~定时任务从 before_request 剥离到后台线程~~（已完成，utils/scheduler.py）
- [x] ~~修复 User bcrypt 循环导入~~（已完成，bcrypt 移到 database 包）
- [x] ~~init_database 改表逻辑迁移到 Alembic 脚本~~（已完成，0003_schema_refactor）
- [x] ~~日志 Windows 轮转冲突 + CSRF 豁免收紧 + options JSON 类型~~（已完成）
- [x] ~~严重问题修复：抽题绕过 + PK 积分丢失 + PK 并发双发~~（已完成，CAS 结算 + assigned_cq_ids 校验，E2E 10/10）
- [x] ~~中等问题修复：Socket.IO 鉴权 + 切屏强制交卷 + 会话校验 + time_spent 服务端计算 + CSRF 兜底 + 题目删除守卫~~（已完成，E2E 13/13）
- [x] ~~轻微问题修复：verify_token 统一 + AI count/options/评分容错 + lobby N+1 + WAITING 过期 + 枚举常量 + onupdate + 错误处理区分~~（已完成，E2E 15/15）
- [x] ~~清理临时测试文件（_check_db.py + tools/ 目录）~~（已完成）
- [x] ~~征集悬赏模块：题目征集/答案征集，发布-投稿-采纳闭环，题目入题库/解析回填，积分+悬赏新星勋章~~（已完成，bounty 包 4 文件 + 9 API + 4 Vue 页面，E2E 21/21）
- [x] ~~日志并发冲突修复：SafeRotatingFileHandler Windows 安全降级 + 高频路径静默 + werkzeug WARNING + Alembic fileConfig 守卫~~（已完成）
- [x] ~~bounty 模块自定义 verify_token 收敛到 utils/security.py 统一版~~（已完成，09-08，9 调用点零改动 + 登录乱码顺带修复）
- [x] ~~生产环境加固：debug 开关环境变量化 + waitress 生产 WSGI + SECRET_KEY 生产强制守卫 + CORS 白名单环境变量化/生产收窄~~（已完成，09-09，v3.3）
- [x] ~~性能与健壮性：过期收卷每考试独立事务+批量预载消除 N+1、AI 代理改请求级、启动数据修正改 SQL 端过滤~~（已完成，09-09，v3.3）
- [x] ~~v3.2 代码推送 GitHub（无关历史处理 + GH013 密钥拦截 + 干净历史重建）~~（已完成，09-08，提交 c6291e2）
- [x] ~~漏洞排查清单专项修复：题库/课程/专业写接口教师鉴权、题库 GET 登录+学生脱敏、通知 IDOR、日志接口鉴权+净化、评分跨考试篡改、save_answer 校验、邀请码学生可见、异常泄露、print 残留、上传 50MB+nosniff、重复 400 handler、重复交卷、query.get 迁移~~（已完成，09-09，v3.4，实测 34/34 + 9/9 + 13/13）
- [x] ~~说明文档更新至 v3.4 + 更新日志同步~~（已完成，09-09）
- [ ] 正式生产部署时配置服务器环境变量（FLASK_DEBUG=false + 强随机 SECRET_KEY + CORS_ORIGINS 前端域名 + FLASK_HOST=0.0.0.0）；可选：仓库添加 .env.example 模板、轮换一次 Ark API Key
- [ ] **上线前**：收紧注册接口角色策略（当前开发阶段允许自选教师/学生，`api/auth_routes.py` 已留注释）——改为仅学生注册或由管理员/教师后台建号
- [ ] **上线前**：评估数据库由 SQLite 切换至 MySQL 8.0 / PostgreSQL / SQL Server 2019+（连接串环境变量化 + 数据迁移脚本，ORM 层业务代码无需改动）；生产环境建议启用 Redis（限流存储现为 memory:// 多进程不共享）
- [x] ~~v3.3 + v3.4 代码提交并推送 GitHub~~（已完成，09-09，提交 75112c8；合并远端网页上传 7407797 后推送，远端 main 为 f54d32c）
- [x] ~~全项目 IDOR/鉴权二次审计（162 端点）：修复考试题目答案泄露（仅创建教师可访问）、悬赏详情答案脱敏（题库题仅教师/快照仅发布者）、成绩详情答案公布时机（考后才公布 + answers_released）；前端学生成绩查询页角色分流、StudentDashboard 图标 CheckCircle→CircleCheck 白屏修复~~（已完成，09-09，v3.5，三场景+三视角实测通过，临时数据已清）
- [x] ~~说明文档更新至 v3.5 + 更新日志同步~~（已完成，09-09）
- [x] ~~v3.5 代码提交并推送 GitHub~~（已完成，09-09，提交 289ea27：7 个代码文件 + 说明文档.md/更新日志.md；app.py 一处 `migate` 笔误已还原未提交，个人笔记 存疑隐患.md 未纳入）
- [x] ~~v4.0 架构切换：Flask-Login 移除 + 9 模板蓝图移除 + templates/static 目录物理删除 + Flask `template_folder=None`/`static_folder=None`~~（已完成，09-14，单端口 SPA 部署）
- [x] ~~v4.0 SPA 托管层：`/assets/*` 长缓存 + catch-all 回退 index.html + `_spa_index_response()` + dist 未构建友好提示~~（已完成，09-14）
- [x] ~~v4.0 新增自主学习模块：5 张表（WrongAnswerRecord/PracticeSession/PracticeAnswer/StudyPlan/StudyLog）+ 17 API + 6 Vue 视图 + 业务钩子 sync_wrong_from_exam 自动收录错题~~（已完成，09-14）
- [x] ~~v4.0 Question 公共题库扩展：creator_id + is_public + scope=all/mine/public + toggle_public + PUT/DELETE 创建者校验~~（已完成，09-14）
- [x] ~~v4.0 考试级联删除修复：Exam→ExamQuestion/ExamStudent/ExamSession/Result + Session→Answer 全 cascade delete-orphan~~（已完成，09-14）
- [x] ~~v4.0 前端功能接入：ExamTake 切屏检测 + CompetitionManage 题目级删除 + Layout 通知单条删除 + smart_composition 智能组卷 + remove_student 移除受邀学生 + 邀请码加入 ?join=CODE~~（已完成，09-14）
- [x] ~~v4.0 品牌重塑：「智汇学场 / Smart Learning Arena」全系统统一（Home/Login/Register/Portal/VideoModal/错误页/文档）~~（已完成，09-14）
- [x] ~~v4.0 VideoModal Canvas 5 幻灯片增强：7 辅助绘制函数（setShadow/clearShadow/drawGlassCard/drawGradientBar/drawGradientCircle/drawGlowRing/drawGradientText）+ 阴影/径向渐变/玻璃拟态~~（已完成，09-14）
- [x] ~~v4.0 蓝图统一注册：19 API Blueprint 通过 `api_bps` 列表循环统一 `url_prefix='/api'`（原 27=9 模板+18 API）~~（已完成，09-14）
- [x] ~~v4.0 说明文档更新至 v4.0 + 更新日志同步~~（已完成，09-14）
- [x] ~~v4.0.1 P0 修复交卷链路：ai_grade_exam 签名 TypeError + 手动交卷永不创建 Result + 错题收录失效 + 切屏 3 次强制交卷逻辑迁移丢失（含前端 force_submitted）~~（已完成，09-15）
- [x] ~~v4.0.1 隐患审计采纳项：SQLite WAL+synchronous=NORMAL；通知 commit 开关与 scheduler 整场原子提交；邀请码 10 次重试上限；日志上报限流 30/min；services.py 形参统一 session；register 补 @log_errors；result_routes logging NameError~~（已完成，09-15）
- [x] ~~v4.0.1 隐患审计驳回项：#6 Redis 降级加进程内 LRU（过度设计、多进程脏数据）；#10 枚举迁移 StrEnum（零约束收益）——#10 记为技术债~~（已评估，09-15）
- [ ] 技术债（评估后暂缓）：枚举类（RoleEnum/ExamStatusEnum/QuestionTypeEnum/DifficultyEnum 及竞赛/悬赏/PK 枚举）若迁移 StrEnum，应连 DB 列一起改 SQLAlchemy Enum + 存量数据校验，单独立项，不做纯风格替换
- [ ] 次要项（低危，后续处理）：`GET /api/exams/<id>` 学生可见未受邀考试元信息（无答案）；question-types/difficulties 枚举接口无鉴权（无敏感数据）
- [ ] 上线前评估：高并发场景 SQLite→PostgreSQL 切换（SQLAlchemy 改动成本低；v4.0.1 已先用 WAL 缓解）

---

## 版本演进总览

| 版本 | 日期 | 核心里程碑 | 状态 |
|------|------|------------|------|
| v1.0 | 2026-06-08 | 项目初始化，Flask + Jinja2 + SQLite 单引擎 | ✅ 已归档 |
| v2.0 | 2026-06-11 | 题型扩展至 8 种 + 教师人工评分 + SQLAlchemy 自动迁移 | ✅ 已归档 |
| v2.1 | 2026-06-25 | 双架构并存：RESTful API + Vue3 前端（Jinja2 模板保留） | ✅ 已归档 |
| v2.9 | 2026-09-05 | 竞赛竞技化三期：Socket.IO 实时推送 + 段位/赛季/勋章/战队/学情画像 | ✅ 已归档 |
| v3.2 | 2026-09-07 | 征集悬赏模块（独立包，师生同权） | ✅ 已归档 |
| v3.3 | 2026-09-09 | 生产部署就绪：waitress WSGI + 环境变量化 + SECRET_KEY fail-fast + CORS 收窄 | ✅ 已归档 |
| v3.4 | 2026-09-09 | 全接口 JWT 鉴权 + 题库/课程/专业写接口教师专属 + 通知改 JWT + 日志净化 + 上传安全 + 异常收敛（34+9+13 E2E） | ✅ 已归档 |
| v3.5 | 2026-09-09 | 考试题目接口仅创建教师可访问 + 悬赏详情答案脱敏 + 成绩答案公布时机 + 前端白屏修复 | ✅ 已归档 |
| v4.0 | 2026-09-14 | Vue3 SPA 全栈化：Flask-Login/Jinja2 全面移除；公共题库；自主学习模块（5 表 + 17 API + 6 视图）；品牌重塑「智汇学场」；Exam 级联删除 | ✅ 已归档 |
| **v4.0.1** | **2026-09-15** | **热修复：交卷链路 P0（签名 TypeError/无成绩/错题收录/切屏强制交卷）；SQLite WAL；通知原子提交；邀请码重试上限；日志限流 30/min；session 形参统一；隐患 #4–#12 逐条评估（采纳 7 驳回 2）** | **✅ 当前最新** |

---

